# subagent_02 复核报告：渲染与排版检查（独立复核）

- 角色：渲染与排版检查 Worker（独立复核者，非作者）
- 被复核成品：
  - `deliverables/group_meeting_20260915/组会汇报_20260915.pdf` — sha256 `fbbce06086e11356cba41ae3064b9ba6d14aad5fa7811f25dbec7988221be0f6`，13 页 / 960×540 pt，12,414,870 bytes，Producer `macOS Quartz PDFContext`，CreationDate 2026-09-15 12:18:12 CST
  - `deliverables/group_meeting_20260915/组会汇报_20260915.pptx` — sha256 `1d6b0e3c712e18553e008d5d5d3f8e432f61021f01a9143373f291c413ddddbd`
- 参考基准（只读）：`build/template_source.pptx`（sha256 `2a5fb4d19e2343820221bb4855b287096bc2baa2e16238dc903bb87c3f0c8b24`，19 页，960×540 pt）、`build/SPEC.md`、`build/SPEC_补充_第二轮修正20260915.md`
- 环境：macOS / python3.9 + python-pptx 1.0.2 + Pillow；poppler 26.04.0（`/opt/homebrew/bin/pdfinfo|pdftotext|pdftoppm`）
- 方法：`pdftotext -bbox / -bbox-layout` 取词级包围盒（PDF 点坐标，原点左上，与 pptx 的 EMU/12700 同系）→ 与 `python-pptx` 读出的形状几何逐页比对；再用 `pdftoppm -r 110` 出 PNG 做像素级连通域/墨迹带复核。
- 写入：本文件（唯一交付文件）。**同一内容同时写入两处**：`<agent-workspace>/.cluster/ppt-20260915/subagent_02.md` 与 `<repo>/.cluster/ppt-20260915/subagent_02.md`（因 plan.md 与 subagent_01/04 落在前者、subagent_03 落在后者，路径口径不唯一，故双写同一文件）。除本文件外未修改仓库任何文件；复核脚本全部写在 agent 工作区 `.openclaw/tmp/ppt-20260915/`（仓库外）。
- 判定口径：高 = 渲染可见且影响阅读/理解；中 = 渲染可见但轻微；低 = 不可见 / 仅 XML 层。标注「实测」= 由 PDF 渲染坐标或像素直接测得；「估算」= 由几何/字体度量推算，换渲染引擎可能变化。

---

## 1. 体检结论摘要

| 项 | 实测结果 | 判定 |
|---|---|---|
| 页数 | 13（`Pages: 13`） | 通过 |
| 页面尺寸 | 960 × 540 pt（= 13.333 × 7.5 in，16:9）；pptx `slide_width/height` = 960.000/540.000 pt，与模板完全一致 | 通过 |
| 词盒越出页面可视区（x<0 / y<0 / x+w>960 / y+h>540） | **0 个** | 通过 |
| 词盒越出所属文本框（>2pt；已剔除表格词与组内子形状的误配） | 25 处词级命中，分布在 15 个形状上；**全部落在无填充/无描边的透明文本框内，或落在更大的可见卡片容器内 → 无可见裁剪**（最大 +38.40pt，P10 文本框 10；其次 P7 文本框 13 +26.0、文本框 10 +20.4） | 通过（低） |
| 文本框两两重叠面积 > 15% | 全篇 85 对命中，逐对归因后 **无「非预期文本框互压」**：12 对在画布外不可见；63 对是「透明文本框 ⊂ 实心卡片/标签框」包含型（正常）；10 对是模板装饰组（P2/P4 的 `矩形: 圆角 4`×`组合 6`、P13 的 `任意多边形 27`/`组合 6`/`图片 10`），几何与模板逐 EMU 一致（继承） | 通过 |
| 文字-文字互相压字（词盒交叠 > 25% 较小面积） | **0 对** | 通过 |
| 文本是否被裁剪/丢失（pptx 文本 vs PDF 渲染文本逐形状覆盖率） | 未渲染文本 16 处，**全部来自 27 个画布外形状中的有字形状**（`DEP`/`角度折叠`/`-180°`/`有效角度误差区间…` 等）；**画布内文本 0 丢失** | 通过 |
| 阅读顺序 P12 | 问题一 y=111.70 → 问题二 y=234.10 → 问题三 y=356.50，间隔均 122.40pt，各自 2 行 | 通过 |
| P11 三阶段标签是否单行 | 第一步/第二步/第三步 **各占 1 行**（16pt） | 通过 |
| P5 第 2 步标签是否单行 | `矩形 31` =「DINO 语义分支」**1 行**（x 235.08–357.58，122.5pt） | 通过 |
| 其余标签类形状是否单行 | P3 流程标签 4 个（抓斗图像／检测框(OBB)/中心／分量观测与连续输出／防摇控制量(后续)）、P5 五步标签 5 个、P6 卡片标题+状态 8 个、P8 卡片标题+状态 12 个 **全部 1 行** | 通过 |
| 表格行列完整性与渲染 | 表格 6 = 11×4、表格 11 = 10×4、表格 17 = 2×3，**声明的行全部渲染出来，无被裁掉的整行**；三张表均无列横向溢出（渲染行宽均 < 列带上界） | 通过 |
| 问题计数 | 高 **1**、中 **5**、低 **7**（含 1 组 27 形状的画布外清单） | — |

> 一句话结论：**没有任何「文字被裁掉/压字/越出页面」的硬缺陷**；主要缺陷集中在 P11 的视觉断裂、P7 表格的孤字行与底部贴边、P5/P8 的行首标点与英文长词断字。

---

## 2. 问题清单

### 2.1 高严重度（渲染可见且影响理解）

#### H-1 | P11 「下一阶段计划」三阶段流程视觉断裂（实测）

三条独立证据，均指向同一后果：页面上看不出「三步流程」的因果链。

1. **三个阶段标签不在同一水平带**（实测，PDF 行坐标）：

   | 元素 | 标签文字行 y（pt） | x（pt） | 文本框矩形（pt） |
   |---|---|---|---|
   | 圆角矩形 4 | 26.16 – 42.83 | 445.44 – 589.44 | x 421.05–613.81，y 6.78–60.78 |
   | 圆角矩形 5 | 88.32 – 104.99 | 176.74 – 368.74 | x 166.54–378.94，y 68.93–122.93 |
   | 圆角矩形 6 | 88.08 – 104.75 | 766.22 – 910.22 | x 756.14–920.30，y 68.62–122.62 |

   第一个标签比另外两个**高 62.1pt（≈0.86 in）**。注：该纵向差是**模板继承**（对模板 P10 逐形状比对 `dx/dy = 0`），非本轮改动引入；但在模板里它靠 `图片 12`（x 108.28,y 121.33,w 378.84,h 245.51）与 `图片 10`（x 601.07,y 154.17,w 247.84,h 317.20）提供流程图形，而这两张图在本版被删除。

2. **页面中部约 290pt 纯空白**（实测，PDF 行 + 像素连通域双重确认）：正文最后一行止于 y=168.6，页脚新增文本框首行从 y=481.26 起 → 空白带 y 168.6→481.26 = **312.7pt（页高 57.9%）**；对该页 PNG 做连通域分析（`thr<200, area≥60`），y∈(190, 480) 区间**没有任何墨迹组件**。

   ```
   ===== P11 =====
     [BIG-GAP] 312.7 pt of blank between y=168.6 and y=481.3
     content y: 26.2 .. 520.9  (top margin 26.2, bottom margin 19.1)
   ```

3. **唯一可见的连接要素指向空白区**（实测）：`直线箭头连接符 27`（x 287.60–288.40，y 108.70–151.84）起点落在 `圆角矩形 5` 内部（框底 122.93，箭头起点比框底高 14.2pt，箭头穿过框底描边），终点 y=151.84 指向下方空白；模板中另外两根 `肘形连接符 18`（y −51.82–186.88）、`肘形连接符 21`（y −1.54–136.91）都从画布上方开始，其中 21 与 `圆角矩形 4` 有 39.0% 面积相交。像素连通域分析显示：页眉分隔线 `直接连接符 3`（y=74.05，x 0–960，线宽 2.25pt）与 圆角矩形 4/5/6、肘形连接符 18/21、箭头 27 连成**同一个连通组件**（`x=0.0–959.6, y=5.9–151.2, area=12436`），即分隔线确实从 第二步/第三步 标签框的上部（框顶下方约 5pt）横穿而过。

- 复现命令：`pdftotext -bbox-layout` + `lines.py 11`；`pdftoppm -r 110 -png` + `blobs.py 11 200 60`；`compare_template.py`。
- 性质说明：**几何是模板继承 + SPEC 明确授权删图**（SPEC 第 2 节 P11：“删除两张图片：`图片 12`、`图片 10`（若要保留视觉平衡，可保留 `图片 12`，但报告里说明）”）。因此这不是违规，而是**授权动作未被视觉兜底**：删除后未重排三标签、未上移页脚、未补任何流程图形/箭头。建议择一：保留 `图片 12`（SPEC 允许）／把三标签拉到同一水平并补 2 根连接箭头／把底部说明与页脚上移到内容区，压缩空白。

---

### 2.2 中严重度（渲染可见，影响观感与易读性）

#### M-1 | P7 表格出现「1–2 字孤字行」与词语被拦腰断开（实测）

| 位置 | 单元格文本 | 渲染行 | 证据（行盒 y / x，pt） |
|---|---|---|---|
| 表格 6 [3,2] | `选择并冻结公式形式` | 行1 `选择并冻结公式形` + 行2 **`式`** | 行2 y 175.93–202.09 内，x 295.10–307.10 |
| 表格 6 [4,2] | `固定模型的结果报告` | 行1 `固定模型的结果报` + 行2 **`告`** | x 295.10–307.10 |
| 表格 6 [8,2] | `时序抖动 / 角度一致性` | 行1 `时序抖动/角度一` + 行2 **`致性`** | 行2 y 355.2–367.2，x 289.10–313.10 |
| 表格 11 [6,2] | `相邻帧对角线相对抖动率` | 行1 `相邻帧对角线相对抖` + 行2 **`动率`** | 行2 y 290.9– …，x 726.80–750.80 |

- 复现：`tblchk2.py 7`（见第 4 节），或 `final_checks.py` 的 `[ORPHAN-1-2CHAR]` 段。
- 备注：`表格 6[7,1]='↑'`、`表格 6[10,1]='—'` 等单字符行是**符号单元格**，不算缺陷；表头 `方向/含义/来源/帧数/职责` 是短表头，也不算缺陷。

#### M-2 | P5 三处行首标点（避头尾失效，实测）

```
===== P5 =====
  [LEAD-PUNCT] y=212.2 x=218.9  '，scope外保留BrightAug'      ← 矩形 32 第 2 行
  [LEAD-PUNCT] y=212.6 x=55.3   '，target仅用于诊断'          ← 矩形 28 第 2 行
  [LEAD-PUNCT] y=483.6 x=298.9  '，不称总体最优'              ← 矩形 58 第 2 行
```

全角逗号被排到行首。第 3 处尤其明显：页脚口径框内第二行只有「，不称总体最优」，且该行底（y 500.27）比 `矩形 58` 声明框底（495.10）低 5.17pt（因文本框无填充/无描边，不产生可见溢出，但断行位置可见）。复现：`final_checks.py`。

#### M-3 | P8 卡片正文把英文标识符从中间截断（实测）

`矩形 71`（V5.2 卡正文，框 x 299.55–448.80，y 204.81–251.31）渲染为 4 行：

```
y=208.29  主候选
y=226.29  split_existing_featur
y=244.29  es未通过，仅保留消融
y=262.29  报告。
```

`split_existing_features` 被断成 `…featur` / `es…`（该标识符是结论主语，误读风险实打实）。同类：`矩形 66` 第 4 行 `不称最优。`、`矩形 76` 第 3 行、`矩形 81` 第 3 行都是「正文第 4 行」溢出 `矩形 66/71/76/81` 的声明框底（分别 +24.85 / +24.98 / +7.04 / +6.94 pt）。**但四张卡片容器 `圆角矩形 63/120/121/122` 的框底为 y=308.07，所有正文行（最低 276.29）与状态标签行（最低 298.46）都在容器内 → 无可见溢出**，故只按「断字可见」判中。复现：`wrap_check.py 8`、`lines.py 8`、`pixels.py 8`。

#### M-4 | P7 表格实际渲染高度远超 frame，且表格 17 贴到页面底边（实测）

| 表 | frame 矩形（pt） | 声明的行高之和（pt） | 渲染范围（pt） | 渲染高度 | 相对 frame 底 |
|---|---|---|---|---|---|
| 表格 6 | x 23.35, y 106.16, w 445.25, h 236.92 | 191.62 | y 110.99 – 425.71 | 314.72 | **超出 frame 底 82.63pt**（超出「行高和」底 127.93pt） |
| 表格 11 | x 468.60, y 103.16, w 470.60, h 290.80 | 214.99 | y 109.31 – 390.19 | 280.88 | 未超出 frame 底（余 3.77pt） |
| 表格 17 | x 161.92, y 434.16, w 647.73, h 62.45 | 62.45 | y 440.22 – 530.26 | 90.04 | 超出 frame 底 33.65pt |

- 表格 17 渲染底 y=530.26 ⇒ **距页面底边仅 9.74pt（0.135 in）**（实测）。同时 `表格 17` 相对模板整体下移 `dy=+31.54pt`（实测，`compare_template.py`），而 `表格 6` 又向下长高 82.6pt，两者渲染间隙只剩 `440.22−425.71 = 14.51pt`。
- 「估算」部分：行高由 PowerPoint 按内容自动撑高，当前**未相撞**（词级碰撞 0 对）；但 14.5pt 与 9.7pt 的余量都很薄，若换字体/换渲染引擎（CJK 行高差异常达 3–6%）或再长一个字，表格 6 与表格 17 有相撞风险。建议把 `表格 17` 上移回模板位置（`dy=0`）或缩小表格 6 列 2 的换行量。

#### M-5 | P5 已完成面板首段意外折行，左右两栏行数不平衡（实测）

`矩形 51`（x 95.67, y 321.68, w 268.50, h 61.49）声明 3 段，实际渲染 **4 行**：

```
y=324.73  分量化观测：数值/有效性/状态/来源/
y=342.73  帧龄/风险
y=361.69  无GT的逐帧在线观测CSV导出
y=379.69  统一观测运行时+整体报告      ← 溢出自身框底 12.01pt
```

右栏 `矩形 54/55`（在 `圆角矩形 52` 内）只有 3 行。左栏第 4 行虽仍在可见容器 `圆角矩形 49`（y 276.83–412.44）内 → 无可见溢出，但面板内 4 行 vs 右面板 3 行，且「帧龄/风险」被单独挤到一行，左侧视觉更挤。复现：`wrap_check.py`、`dump_shape.py 5 "矩形 51"`。

---

### 2.3 低严重度（不可见 / 仅 XML 层）

| 编号 | 页 | 元素 | 证据（数值） | 说明 |
|---|---|---|---|---|
| L-1 | P5/P6/P8/P11 | **27 个画布外形状全部保留**（模板继承） | 见 §2.4 清单；`compare_template.py` 对每个都标 `[in-template:OFF-CANVAS]` | 27 个形状（含 10 个无字形状）在原模板中即已越界，**本轮新引入越界形状 0 个**；其中有字的 16 个形状文字**不进入 PDF 渲染**，不产生可见缺陷 |
| L-2 | P3/P5/P8/P10/P11/P13 | 透明文本框文字越出自身框 | P3 `矩形 9` +8.01；P5 `矩形 50` +3.80 / `矩形 53` +2.23 / `矩形 51` +12.01 / `矩形 57` +5.76 / `矩形 58` +5.17；P8 `矩形 66` +24.85 / `矩形 71` +24.98 / `矩形 76` +7.04 / `矩形 81` +6.94；P10 `文本框 10` +38.40；P11 `文本框 新增第三步之后` +2.45；P13 `文本框 15` 顶部 −3.16 | 这些形状 spPr 均为 `<a:noFill/>` + `<a:ln w="0"><a:noFill/></a:ln>`（无填充无描边），或外层还有实心卡片容器兜住 → 渲染不可见 |
| L-3 | P6 | 唯一「可见画布内」的 >15% 重叠对在画布外 | `矩形 46` × `矩形 60` = 16.8%（较小面积） | 两者均在 y<0 的画布外组内，不可见 |
| L-4 | P11 | `圆角矩形 5` × `直线箭头连接符 27` 几何相交 39.0%（对较小者）；箭头起点在框内 14.2pt | `overlap.py`、`blobs.py 11 200 60` | 几何与模板一致（`dy=0`），**继承非新引入**；删图后缺少背景图形，观感更像“漏画” |
| L-5 | P7 | 表头 `文本框 10`、`文本框 13` 文字宽于自身框 | 渲染 x 35.3–176.1 vs 框 28.11–155.71（右溢 20.4pt）；x 487.2–628.0 vs 框 480.00–602.00（右溢 26.0pt） | 文本框无填充；未与表体相撞（表体首行 y=109.3/110.99 在表头 y 79.6–99.2 之下）；词级碰撞 0 对 |
| L-6 | P12 | 三问块结束于 y=435.6，底部余 104.0pt 空白（顶部标题→Q1 仅 43.2pt 间隙） | `final_checks.py` P12：`content y: 32.5 .. 400.5` | 补充稿要求“均匀铺满页面”；三块**间隔均匀**（122.4pt）✓，仅整体略偏上，属观感级「估算」 |
| L-7 | P6 | 卡片区底 y=295.1 到页脚注 y=419.3 有 124.2pt 空白 | `final_checks.py`：`[BIG-GAP] 124.2 pt …` | 与模板一致（`矩形 61` 未位移），非本轮引入（P1 封面 292.9pt 空白同理，属封面设计） |
| L-8 | P11 | 第三步标签框内可用宽度余量仅 2.88pt/侧 | 文本 144.0pt（766.22–910.22）vs 文本区 149.76pt（763.34–913.10） | 「估算」：换字体/换度量即可能折行，建议留 ≥8pt 余量 |

---

### 2.4 与模板对比：成品中「原本在模板画布外、仍被保留」的形状（全 27 个）

判定式：`x<0 或 y<0 或 x+w>960pt 或 y+h>540pt`（= 13.333×7.5 in）；单位 pt。**全部 `[in-template:OFF-CANVAS]`**（同一形状在模板对应页也越界，`compare_template.py` 逐名比对）。模板 19 页共有 30 个画布外形状，另 3 个（`T11 肘形连接符 25/28`、`T14 图片 10`）位于被删除的模板页上。

| 成品页 ← 模板页 | 形状 | x | y | w | h | 有文字？ |
|---|---|---|---|---|---|---|
| P5 ← T9 | 矩形 59 | 98.67 | **600.82** | 750.00 | 25.50 | 有（`这套规则避免 test 泄漏…`，不渲染） |
| P6 ← T7 | 圆角矩形 42 | 506.64 | −158.90 | 206.25 | 153.75 | 无 |
| P6 ← T7 | 矩形 43 | 520.14 | −148.40 | 179.25 | 24.00 | 有 `DEP` |
| P6 ← T7 | 矩形 44 | 520.14 | −121.40 | 179.25 | 21.00 | 有 `深度估算误差传播率` |
| P6 ← T7 | 矩形 45 | 520.14 | −94.40 | 179.25 | 57.75 | 有 |
| P6 ← T7 | 矩形 46 | 275.39 | −56.98 | 179.25 | 16.50 | 有 `越小越好` |
| P6 ← T7 | 矩形 47 | **−147.25** | −68.43 | 127.50 | 25.50 | 有 `角度折叠` |
| P6 ← T7 | 直线连接符 48 | **−1.00** | −36.93 | 498.75 | 0.75 | 无 |
| P6 ← T7 | 直线连接符 49 | **−1.00** | −47.43 | 0.75 | 21.00 | 无 |
| P6 ← T7 | 矩形 50 | **−52.75** | −26.77 | 52.50 | 18.00 | 有 `-180°` |
| P6 ← T7 | 直线连接符 51 | 123.50 | −47.43 | 0.75 | 21.00 | 无 |
| P6 ← T7 | 矩形 52 | 71.75 | −26.77 | 52.50 | 18.00 | 有 `-90°` |
| P6 ← T7 | 直线连接符 53 | 248.75 | −47.43 | 0.75 | 21.00 | 无 |
| P6 ← T7 | 矩形 54 | 197.00 | −26.77 | 52.50 | 18.00 | 有 `0°` |
| P6 ← T7 | 直线连接符 55 | 373.25 | −47.43 | 0.75 | 21.00 | 无 |
| P6 ← T7 | 矩形 56 | 321.50 | −26.77 | 52.50 | 18.00 | 有 `90°` |
| P6 ← T7 | 直线连接符 57 | 497.75 | −47.43 | 0.75 | 21.00 | 无 |
| P6 ← T7 | 矩形 58 | 446.00 | −26.77 | 52.50 | 18.00 | 有 `180°` |
| P6 ← T7 | 圆角矩形 59 | 122.75 | −75.93 | 251.25 | 28.50 | 无 |
| P6 ← T7 | 矩形 60 | 122.75 | −69.93 | 251.25 | 18.00 | 有 `有效角度误差区间：[-90°, 90°]` |
| P8 ← T8 | 圆角矩形 42 | 506.64 | −158.90 | 206.25 | 153.75 | 无 |
| P8 ← T8 | 矩形 43 / 44 / 45 | 520.14 | −148.40 / −121.40 / −94.40 | 179.25 | 24.00 / 21.00 / 57.75 | 有 |
| P8 ← T8 | 矩形 46 | 520.14 | −29.15 | 179.25 | 16.50 | 有 `越小越好` |
| P11 ← T10 | 肘形连接符 18 | 615.81 | **−51.82** | 41.95 | 238.70 | 无（下段 y 0–186.88 可见） |
| P11 ← T10 | 肘形连接符 21 | 427.08 | **−1.54** | 42.26 | 138.45 | 无（y 0–136.91 可见） |

- 与 `build/verify_report.txt` 的差异：该报告写「模板原本就画布外的形状 **26** 个」，本复核按上述判定式得 **27** 个（差 1，疑为其计数/阈值口径不同，其日志在打印中途被截断，无法比对具体是哪一条）。以本报告 27 个为准（逐个可复现）。
- 附带（实测，非缺陷）：成品相对模板的几何改动只有 9 处 —— P1 `文本框 3` 加宽 +83.20pt；P6 `矩形 35` 加宽 +10.86（左移 5.87）；P7 `表格 17` 下移 +31.54；P11 `圆角矩形 4/5/6` 高度各 +14.23、`圆角矩形 5` 左移 31.27 并加宽 31.21、`圆角矩形 6` 加宽 42.05、`文本框 30/8` 加宽 359.16/272.40 且加高 50.12；P12 `文本框 1` 下移 18.70、加高 6.50。**SPEC_补充只授权了 P11 的宽度/字号变化，未提及三框高度 +14.23pt、P6 矩形 35、P7 表格 17 下移** —— 这三处属「未在补充件中说明的几何改动」，请主线程向作者确认是否有意为之（本条属事实陈述，是否算违规由仲裁判定）。

---

## 3. 未能执行的检查及原因

1. **人眼/多模态视觉复核未做**：`image` 工具返回 403 `Auto-Fast free quota used up. Subscribe to a membership to continue.`（错误原文），本会话无法调用视觉模型。已改用**像素级连通域/墨迹带分析**（`blobs.py`、`pixels.py`，`pdftoppm -r 110 -png` 输出，本地 Pillow 计算）替代——足以判定「某元素是否可见、落在何处、是否连通」，但**无法评价审美质量**（配色是否刺眼、图形是否好看）。
2. **字号/字体/配色/主题合规性未查**：`pdftotext -bbox` 不输出字号，本 worker 也未解析字体表；该项属 subagent_03（模板继承与规范）范围。
3. **「PDF 是否由这份 pptx 导出」无法直接证明**：无 PowerPoint 导出日志可用。间接证据：pptx 全部文本在 PDF 中逐形状命中（除 16 处画布外文本）→ 二者内容一致；Producer = `macOS Quartz PDFContext` 与 `build/export_pdf.sh` 记录的 AppleScript 导出路径相符（未复核脚本本身）。
4. **表格的行高/垂直对齐/边框线型/单元格合并**未查：python-pptx 只能给出「声明行高」，实际行高由渲染撑开，二者不一致（见 M-4），故本报告只报「渲染实测值 vs 声明值」的差，不对「应当多高」下判断。
5. **换渲染引擎后的折行复现**未做（缺 LibreOffice/PowerPoint 自动化）：M-1/M-4/L-8 中标注「估算」的余量风险，只能给出当前渲染的实测余量，不能证明其它引擎下的表现。
6. **未复核**：数字与结论的正确性（subagent_01）、讲稿/备注一致性（subagent_04）、SPEC 文字逐字符合性中属于内容层面的部分。

---

## 4. 运行过的完整命令与关键输出

工作目录（仓库外，全部脚本落此）：

```
W=/Users/mac/.openclaw-autoclaw/agents/auto-coder/workspace/.openclaw/tmp/ppt-20260915
P=/Users/mac/Documents/paper/symEOOD/deliverables/group_meeting_20260915/组会汇报_20260915.pdf
export PATH=/opt/homebrew/bin:$PATH
```

### 4.1 页数与页面尺寸

```bash
pdfinfo "$P" | egrep "Pages|Page size|Producer|CreationDate"
# Pages: 13        Page size: 960 x 540 pts
# Producer: macOS 版本26.6.1（版号25G76） Quartz PDFContext
# CreationDate: Tue Sep 15 12:18:12 2026 CST
python3 -c "import pptx;print(pptx.__version__)"      # 1.0.2
```

```bash
python3 - <<'EOF'
from pptx import Presentation
p=Presentation("/Users/mac/Documents/paper/symEOOD/deliverables/group_meeting_20260915/组会汇报_20260915.pptx")
print(len(p.slides), p.slide_width/12700.0, p.slide_height/12700.0)   # 13 960.0 540.0
EOF
```

### 4.2 词级包围盒导出 + 越界检查

```bash
pdftotext -bbox        "$P" $W/bbox.html
pdftotext -bbox-layout "$P" $W/bbox_layout.html
grep -c "<word" $W/bbox_layout.html    # 438
grep -c "<line" $W/bbox_layout.html    # 227
grep -c "<block" $W/bbox_layout.html   # 156
grep -c "<page " $W/bbox_layout.html   # 13
python3 $W/analyze.py   > $W/analyze_out.txt   # 含 [A-OFFPAGE] 检查
grep -c "A-OFFPAGE" $W/analyze_out.txt         # 0   ← 无词盒越出页面
```

### 4.3 词盒 vs 所属文本框 / 重叠 / 孤字行（核心脚本）

```bash
python3 $W/analyze2.py  > $W/a2.txt    # 文本匹配 + 几何兜底定位 owner，>2pt 才报
python3 $W/analyze3.py  > $W/a3.txt    # 形状级文本 bbox vs 形状盒；词级碰撞
python3 $W/final_checks.py             # 行首标点 / 1–2 字孤字行 / 大空白带
python3 $W/wrap_check.py               # 段数 vs 渲染行数（意外折行）
python3 $W/coverage.py                 # pptx 文本 vs PDF 渲染文本覆盖率
python3 $W/overlap.py                  # 形状两两重叠 >15%
```

关键输出（节选，原文照录）：

```
##### WORD vs OWNER SHAPE #####            (analyze.py, 阈值 1pt)
-- P2: 1 word-box violations     文本框 10  '研究问题与动机' over T1.0 B-3.5      ← 以内，忽略
-- P3: 3 word-box violations     矩形 9 '现有方案…' over B8.0
-- P5: 4 word-box violations     矩形 50 B3.8 / 矩形 53 B2.2 / 矩形 57 B5.76 / 矩形 58 B5.17
-- P8: 5 word-box violations     矩形 66 B6.85(+行4 24.85) / 矩形 71 B6.98 / 矩形 81 B6.94
-- P10: 文本框 10  '…EKF 协方差。' B37.0
##### SHAPE PAIRWISE OVERLAP (text shapes) >15% #####
   P6   矩形 46 x 矩形 60   inter=497.9pt2  16.8% of smaller     ← 两者均画布外
```

```
===== P7 =====
  [ORPHAN-1-2CHAR] y=190.1 x=295.1 '式'
  [ORPHAN-1-2CHAR] y=225.9 x=295.1 '告'
  [ORPHAN-1-2CHAR] y=290.9 x=726.8 '动率'
  [ORPHAN-1-2CHAR] y=355.2 x=289.1 '致性'
===== P11 =====
  [BIG-GAP] 312.7 pt of blank between y=168.6 and y=481.3
===== P12 =====
  content y: 32.5 .. 400.5  (top margin 32.5, bottom margin 139.5)
```

```
shape/cell texts NOT found in rendered page text: 16     (coverage.py)
   （全部 off_canvas=True：P5 矩形 59；P6 矩形 43/44/45/46/47/50/52/54/56/58/60；P8 矩形 43/44/45/46）
total pairs >15%: 85                                     (overlap.py)
   → 12 对画布外；63 对「透明文本框 ⊂ 实心卡片/标签框」（≥98% 包含，正常）；10 对模板装饰组（P2/P4/P13）
```

```
# 词盒越出所属文本框（>2pt），剔除表格词/组内子形状误配后的最终口径：
FINAL (table words excluded): hit shapes=15  hit word-pairs=25
  P3  矩形 9        n=1 max= +8.01   P5 矩形 50 n=1 max= +3.80   P5 矩形 51 n=3 max=+12.01
  P5  矩形 53       n=1 max= +2.18   P5 矩形 57 n=1 max= +5.75   P5 矩形 58 n=1 max= +5.17
  P7  文本框 10      n=1 max=+20.36   P7 文本框 13 n=1 max=+25.96
  P8  矩形 66 n=2 max=+24.86  矩形 71 n=3 max=+24.99  矩形 76 n=2 max=+7.05  矩形 81 n=2 max=+6.94
  P10 文本框 10      n=4 max=+38.40  P11 文本框 新增第三步之后 n=1 max=+2.50  P13 文本框 15 n=1 max=+3.16
```

### 4.4 表格取证

```bash
python3 $W/tblchk.py 7  > $W/tbl2.txt      # 初版（列带误包含相邻表，已被 tblchk2 修正）
python3 $W/tblchk2.py 7 > $W/tbl3.txt      # 按 x 重叠挑最近下表，x 容差 +2pt
```

关键输出：

```
===== 表格 6  frame x=23.35..468.60 y=106.16 declared h=191.62 (clip y<434.16)
  col bounds: [23.35, 153.27, 245.27, 356.93, 468.6]
  rendered rows=11 (declared 11) top=110.99 bottom=425.71 height=314.72 [frame bottom=297.78]
   row3 col2 x= 295.10- 307.10 '式'   <<ORPHAN-LINE(1)
   row4 col2 x= 295.10- 307.10 '告'   <<ORPHAN-LINE(1)
   row8 col2 x= 289.10- 313.10 '致性' <<ORPHAN-LINE(2)
===== 表格 11 frame x=468.60..939.20 y=103.16 declared h=214.99
  rendered rows=10 (declared 10) top=109.31 bottom=390.19 height=280.88
   row6 col2 x= 726.80- 750.80 '动率' <<ORPHAN-LINE(2)
   —— 无任何 COL-OVERFLOW（全部渲染行 x1 ≤ 列带右界）
===== 表格 17 frame x=161.92..809.65 y=434.16 declared h=62.45
  rendered rows=2 (declared 2) top=440.22 bottom=530.26 height=90.04
```

### 4.5 模板对比（画布外清单与几何 delta）

```bash
python3 $W/compare_template.py > $W/cmp.txt
sed -n '/DELIVERABLE OFF-CANVAS/,/GEOMETRY DELTAS/p' $W/cmp.txt | grep -c "\[in-template"   # 27
sed -n '/TEMPLATE OFF-CANVAS/,/DELIVERABLE OFF-CANVAS/p' $W/cmp.txt | grep -c "^  T"          # 30
# GEOMETRY DELTAS (>0.5pt) 共 9 行，见 §2.4 附带说明
```

### 4.6 像素级复核（替代视觉模型）

```bash
pdftoppm -r 110 -png -f 1 -l 13 "$P" $W/png/p        # 13 张 1467×825
python3 $W/pixels.py 5   # ASCII 墨迹图（1 cell = 8×16 px ≈ 5.2×10.5 pt）
python3 $W/pixels.py 11  # 可见：P11 y≈190–480 全空；页眉分隔线横穿 第二步/第三步 框
python3 $W/blobs.py 11 200 60   # 连通域：x=0.0-959.6 y=5.9-151.2 area=12436（分隔线+4框+两肘形连接符+箭头 相连）
sha256：fbbce060…1be0f6（pdf）/ 1d6b0e3c…ddddbd（pptx）/ 2a5fb4d1…f0c8b24（模板）
```

---

### 附：本报告使用的脚本清单（均在仓库外 `$W`）

| 脚本 | 作用 |
|---|---|
| `analyze.py` | 逐页词盒越页检查、owner 形状越界、形状两两重叠、孤字行、画布外形状 |
| `analyze2.py` | owner 定位（文本匹配+几何兜底）、页内块阅读顺序（y 排序） |
| `analyze3.py` | 形状级文本 bbox vs 形状盒、词级碰撞、填充/描边摘要 |
| `dump_shape.py` | 单形状几何/段落/字号/边距 + 交叠的渲染行 |
| `lines.py` | 逐页渲染行（y/x/文本）全量列表 |
| `tblchk.py` / `tblchk2.py` | 表格：列带 vs 渲染行、行簇、列溢出、孤字行 |
| `wrap_check.py` | 段数 vs 渲染行数（意外折行） |
| `final_checks.py` | 行首标点、1–2 字孤字行、大空白带、上下留白 |
| `coverage.py` | pptx 文本 vs PDF 渲染文本覆盖率（证「无画布内文本丢失」） |
| `overlap.py` | 全形状两两重叠 >15%（区分画布内外） |
| `compare_template.py` | 成品 vs 模板：画布外清单、几何 delta、增删形状 |
| `pixels.py` / `blobs.py` | PNG 墨迹 ASCII 图 / 连通域分析（替代视觉模型） |
