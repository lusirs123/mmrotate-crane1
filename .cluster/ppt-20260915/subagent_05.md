# subagent_05 终版独立复验报告（同一套方法复查修正后的终版）

- 角色：终版独立验证 Worker（**只读复核者，非制作者**）。结论只采信我自己的实测，不采信制作方自述。
- 复核对象（终版成品）：
  - `deliverables/group_meeting_20260915/组会汇报_20260915.pdf` — sha256 `48ed01efe6e5f0a4db5849a9e5121a38ce97578b35210c9c53858b487a9b1da9`，12,405,994 bytes，CreationDate **2026-09-15 12:50:49 CST**，Producer `macOS Quartz PDFContext`
  - `deliverables/group_meeting_20260915/组会汇报_20260915.pptx` — sha256 `3de4449d4b8e50bbb566ba168629087a5bbb941df1ef274fc07cadfe9011c8fc`
  - 参考模板（只读）：`build/template_source.pptx` — sha256 `2a5fb4d19e2343820221bb4855b287096bc2baa2e16238dc903bb87c3f0c8b24`（与上一轮一致）
- 环境：macOS / python3 + python-pptx 1.0.2 + Pillow；poppler 26.04.0（`/opt/homebrew/bin/pdftotext|pdftoppm|pdfinfo`）；ghostscript 10.07.1。
- 方法：`pdftotext -bbox-layout` 取词/行包围盒（PDF pt，原点左上，1pt=1/72in）→ 与 `python-pptx` 形状几何逐页比对；`pdftoppm -r 110 / -r 300` 出 PNG 做像素墨迹/空白带/连通域复核；模板几何逐形状（按名）比对。
- 本报告脚本全部落在**仓库外** `<agent-ws>/.openclaw/tmp/ppt-20260915-fixv/`；**未修改任何被复核文件**。唯一写入：本文件（同时写入仓库 `.cluster/ppt-20260915/subagent_05.md`）。
- 术语：**实测** = 由 PDF 坐标 / 像素直接测得；**估算** = 由几何或字体度量推算，换引擎可能变化。

---

## 0. 一句话结论

终版**未被误伤**（内容差异严格限于 P5/P7/P8/P11；备注差异严格限于 P8/P10/P12），**6 处被修问题全部修好**；**唯一未达成项**是 P11「行3→底部说明」之间仍有 **1.060in** 连续空白带（>第 4 轮自设的 1.0in 阈值，制作方 `qa_fix4_report.txt` 亦自述 ★超★，**与我的实测一致**）。此外 P11 说明文字相对标签的垂直中心偏差约 **+0.03in**，处在 0.03in 口径的临界/略超（与制作方自述的 +0.0333in 方向一致）。

---

## 1. 逐项判定总表

| # | 检查项 | 实测 | 判定 |
|---|---|---|---|
| 1 | 页数 | `Pages: 13` | **达成** |
| 1 | 页面尺寸 | `Page size: 960 x 540 pts`；pptx `960.0×540.0pt` | **达成** |
| 2a | P11 三标签各自单行 | 第一步/第二步/第三步 各 1 行（16pt） | **达成** |
| 2a | P11 与页眉分隔线（y=1.03in）相交 | 分隔线 y=74.05pt=1.0285in；标签框顶 108/216/324pt → 不相交 | **达成** |
| 2a | P11 三行间空白带（行1↔行2 / 行2↔行3） | 墨迹 62.40pt=0.867in / 62.40pt=0.867in（110dpi: 62.18pt=0.864in）；文本层 88.46pt=1.229in / 78.44pt=1.089in | **达成**（内部行距合格） |
| 2a | P11 行3↔底部说明空白带 | **76.32pt = 1.060in > 1.0in** | **未达成**（第 4 轮阈值） |
| 2a | P11 是否还有指向空白的可见箭头 | 三连接符区域墨迹 = 0；唯一横线=分隔线 | **达成**（无可见箭头） |
| 2a | P11 说明文字 vs 左侧标签垂直关系 | 墨迹中心 Δ = +2.16 / +2.28 / +0.84 pt = **+0.030 / +0.032 / +0.012 in**（说明略低） | 临界/略超（口径 0.03in） |
| 2b | P7 表格6/表格11 "1–2 字孤字行" | ≤2 字 CJK 行全部是完整表头单元格（帧数/职责/方向/含义/来源） | **达成**（孤字行已消除） |
| 2b | P7 表格17 距页底 | 最低墨迹 y=515.76pt → **24.24pt = 0.337in** | **达成**（≥0.30in） |
| 2b | P7 两表间隙 | 表格6 底 353.76pt → 表格17 顶 427.20pt = **73.44pt = 1.020in** | **达成**（≥0.12in） |
| 2c | P5 矩形28/32/58 行首标点 | 无任何行以 `，；、。` 开头 | **达成** |
| 2c | P5 矩形51 行数 | 3 行 | **达成** |
| 2d | P8 矩形71 英文标识符断字 | 渲染 2 行（"主候选未通过，仅保留"/"消融报告。"），无被截断标识符 | **达成** |
| 3 | 词盒越出页面可视区数量 | **0**（全 422 词盒） | **达成** |
| 3 | 文本框两两重叠 >15% | 74 对 = 11 画布外 + 63 包含型(≥98%) + **0 画布内非包含** | **达成**（无新可见重叠） |
| 3 | 新出现越界形状（相对模板） | **1**（`直线箭头连接符 27`，SPEC 第 3 轮 F1 授权移出画布）；画布外总数 28 = 继承 27 + 新增 1 | **达成**（有意为之） |
| 4 | 内容未被误伤 | pptx 层 + PDF 层双口径：差异仅 P5/P7/P8/P11，其余 9 页逐字一致 | **达成** |
| 4 | 备注差异范围 | 仅 P8 / P10 / P12 | **达成** |
| 5 | 13 页是否都有备注 | 13/13 页均有备注 | **达成** |
| 5 | 讲稿与备注.md vs pptx 备注 | 13 页逐字一致 | **达成** |
| 6 | 仓库只读（已跟踪文件被改动） | `git diff --stat` 空；全部为未跟踪新增 | **达成** |

---

## 2. 逐项实测证据

### 2.1 页数与页面尺寸（达成）

```
$ pdfinfo 组会汇报_20260915.pdf | egrep "Pages|Page size|Producer|CreationDate"
Pages:           13
Page size:       960 x 540 pts
Producer:        macOS 版本26.6.1（版号25G76） Quartz PDFContext
CreationDate:    Tue Sep 15 12:50:49 2026 CST

$ python3 -c "from pptx import Presentation; p=Presentation('组会汇报_20260915.pptx'); print(len(p.slides), p.slide_width/12700.0, p.slide_height/12700.0)"
13 960.0 540.0
```
→ **13 页 / 960×540pt（=13.333×7.5in, 16:9）**，与模板一致。

### 2.2 被修过的 6 处

#### 2.2.1 P11 三阶段标签（F1 / G1 / G2）

**（a）三标签各自单行**（实测，PDF 文本层行盒）
```
'第一步：冻结与评价'      y0=122.64  y1=139.31
'第二步：小目标与数据补齐'  y0=230.64  y1=247.31
'第三步：泛化与收尾'      y0=338.64  y1=355.31
```
每标签恰好 1 行，行高 ~16.7pt（16pt 字号）。→ **达成**。

**（b）与页眉分隔线是否相交**（实测）
- `直接连接符 3` 几何：T=74.05pt = **1.0285in**，W=960pt，H=0（水平线）；像素墨迹 y=72.65–74.62pt（线宽 ~2pt），x=0–959.56pt。
- 三个标签框（`圆角矩形 4/5/6`，AUTO_SHAPE）几何：T=108 / 216 / 324 pt = **1.50 / 3.00 / 4.50 in**，H=44.64pt。
- 分隔线（~74pt）在全部标签框顶（108pt 起）**上方 ~34pt**，**不相交**。→ **达成**。

**（c）三行之间的连续空白带**（双口径，实测）

| 口径 | 行1↔行2 | 行2↔行3 | 行3↔底部说明 |
|---|---|---|---|
| 墨迹（300dpi 像素，含图形/框线） | 2.127–2.993in = **62.40pt = 0.867in** | 3.627–4.493in = **62.40pt = 0.867in** | 5.127–6.187in = **76.32pt = 1.060in** |
| 墨迹（110dpi，制作方同口径） | 62.18pt = **0.864in** | 62.18pt = **0.864in** | 75.93pt = **1.055in** |
| 文本层（PDF 行盒，文本到文本） | 142.18→230.64pt = **88.46pt = 1.229in** | 250.18→328.62pt = **78.44pt = 1.089in** | 368.26→445.26pt = **77.00pt = 1.069in** |

- 说明：墨迹口径把三个 `圆角矩形` 的框线算作墨迹，故"空白带"是**框与框之间**（≈0.87in）；文本层口径是**文字与文字之间**（≈1.09–1.23in）。两者差异来自标签框线填补了邻近文字的空档。
- **三行内部的两个空档均 ≈0.87in（墨迹口径）< 1.0in → 合格。**
- **但「行3 → 底部说明」之间仍有 1.055–1.060in > 1.0in**（行3 底 5.12in，底部说明首行 6.185in）→ **未达成第 4 轮自设阈值**；制作方 `qa_fix4_report.txt` 亦标此带为 ★超★，**与实测一致**。

**（d）是否还有指向空白的可见箭头**（实测）
- 三个残留连接符 `直线箭头连接符 27`、`肘形连接符 18/21` 的 T 均为 **-144pt = -2.00in**（移出画布上方）。
- 像素探测：`x 285–292/y 76–200`、`x 610–660/y 76–120`、`x 424–472/y 76–120` 三区域 **墨迹像素 = 0**。
- P11 上唯一横贯墨迹是分隔线（x 0–959.56）本身；分隔线以下至行1 之间（y 75.3–107.4pt）**全空**。
→ **页面上已无任何指向空白的可见箭头。达成。**

**（e）说明文字与左侧标签的垂直关系**（实测，像素墨迹中心）
```
行1 标签墨迹中心 130.56pt | 说明墨迹中心 132.72pt | Δ=+2.16pt=+0.0300in
行2 标签墨迹中心 238.44pt | 说明墨迹中心 240.72pt | Δ=+2.28pt=+0.0317in
行3 标签墨迹中心 346.56pt | 说明墨迹中心 347.40pt（说明 2 行）| Δ=+0.84pt=+0.0117in
```
- 三个说明框 `文本框 30 / 8 / 新增第三步说明` 的 `anchor=ctr`（垂直居中），框顶取"标签顶 −0.02in"（1.48/2.98/4.48in），故**设计上两框中心线相差 0.02in**。
- 实测说明文字比标签文字**低约 2.2pt（≈0.03in）**；制作方以其自建基线口径测得 +0.0333in 并标 ★超 0.03in 阈值 0.24pt★ —— **我的墨迹口径（+0.030~+0.032in）与之一致**。
- 判定：**基本对齐（视觉居中），但严格 0.03in 口径下为临界/略超**；此为 16pt 与 18pt 字体度量差 + 0.02in 设计偏置所致。制作方未虚报。

#### 2.2.2 P7 表格（F2 / F5）

**（a）表格6 / 表格11 是否还有"1–2 字孤字行"**（实测）

遍历 P7 两张表区域内全部渲染行，取 ≤2 个 CJK 字符的行：
```
[表格 6]  y=111.37 '帧数'   full-cell=True
[表格 6]  y=111.37 '职责'   full-cell=True
[表格 11] y=109.69 '方向'   full-cell=True
[表格 11] y=109.69 '含义'   full-cell=True
[表格 11] y=109.69 '来源'   full-cell=True
疑似孤字行 = 0
```
- 全部 ≤2 字行的文本均**恰好等于某个完整单元格文本**（表头），并非断行残尾。
- 上一轮的孤字行 `式`（表格6[3,2]）、`告`（[4,2]）、`致性`（[8,2]）、`动率`（表格11[6,2]）**均已消失**：
  - `选择并冻结公式形式 → 公式选型与冻结`（1 行，x259.10–343.10）
  - `固定模型的结果报告 → 固定模型报告`（1 行）
  - `时序抖动 / 角度一致性 → 抖动 / 角度一致性`（1 行）
  - `相邻帧对角线相对抖动率 → 相邻帧对角线抖动率`（1 行）
→ **达成，孤字行已消除。**

**（b）表格17 距页底**（实测）
- `表格 17` 声明 T=421.20pt、H=62.45pt（frame 底 483.65pt），但行高被渲染撑开：末行文本行盒底 y=517.30pt。
- 像素（300dpi）最低墨迹 y=**515.76pt**（表格17 无外框底边，最低墨迹即末行文字下缘）。
- **距页底 = 540 − 515.76 = 24.24pt = 0.337in**（上一版为 9.74pt=0.135in，本轮已抬高）。→ **达成（≥0.30in）**。
- 附带观察（实测）：表格17 渲染高度≈ 427.2→515.8（≈88.6pt）＞声明 frame 高 62.45pt，即**向下溢出自身 frame ~32pt**（自动撑高），但**未触及页面底边**。

**（c）两表间隙**（实测）
- 表格6 渲染底（末行文本墨迹）≈ 353.76pt；表格17 渲染顶（首行文本墨迹）≈ 427.20pt。
- **间隙 = 73.44pt = 1.020in**（文本层口径 73.07pt=1.015in）。→ **达成（≥0.12in）**。

#### 2.2.3 P5 行首标点与矩形51（F3 / F6）

**（a）矩形28 / 矩形32 / 矩形58 行首标点**（实测）
对 P5 全部渲染行做 `^[，；、。]` 匹配：**命中 0 行**。
相关渲染：
```
'只用 source 标注训练'                         （矩形28，1 行）
'恢复暗光排序，其余帧保' / '持原样'             （矩形32，2 行；第2行以"持"开头）
'等覆盖率比较，漏检计入 coverage；V5.1 不称最优' （矩形58，1 行）
```
→ **达成，三处行首标点已消除。**（注：矩形32 仍在"保持"两字间断行，但非行首标点、非孤字，属可接受折行）

**（b）矩形51 行数**（实测）
```
y=324.73 '分量化观测：数值/有效性/状态'
y=342.73 '无GT逐帧在线观测CSV导出'
y=361.69 '统一观测运行时+整体报告'
```
恰 **3 行**（上一版为 4 行且末行溢出 12.01pt）。→ **达成。**

#### 2.2.4 P8 矩形71 英文标识符断字（F4）

**（实测）**
```
y=208.29 '主候选未通过，仅保留'
y=226.29 '消融报告。'
```
`split_existing_features` 已不复现在正文（改写入 P8 备注）。渲染行内**无被拦腰截断的英文标识符**。→ **达成。**
（旁证：P8 其余卡片 `矩形66/76/81` 末行分别为 5/10/9 字，均非 1–2 字残尾。）

### 2.3 是否引入新问题

**（a）词盒越出页面可视区**（实测）
```
total words = 422
words with x0<-0.5 or y0<-0.5 or x1>960.5 or y1>540.5 = 0
```
→ **0 个**（与上一轮一致）。**达成。**

**（b）文本框两两重叠 >15%**（实测，较小面积口径）

对每页做两两相交计算（含所有有面积形状）：
```
重叠>15% 归类: {'offcanvas': 11, 'containment>=98%': 63, 'oncanvas_partial': 0}  合计 74
```
- **11 对**：双方均在画布外（P6/P8 的 y<0 组），不可见。
- **63 对**：一方完全包含在另一方内（包含率 ≥98%），典型为"透明文本框 ⊂ 实心卡片/标签框"（如 P5 `圆角矩形 49 ⊂ 矩形 51`、`圆角矩形 26 ⊂ 矩形 27`；P3 `矩形 12 ⊂ 矩形 13/15/…`；P2/P4 `矩形: 圆角 4 ⊂ 组合 6`），几何与模板继承一致，**属正常结构**。
- **0 对**：画布内、且非包含关系（即"真正互压"）的文本框重叠。→ **无新可见重叠。达成。**
- 说明：跨版本"对数"口径可比性受限（我按"全部有面积形状"计得 74，上一轮报告按不同过滤口径记 85）；关键一致性在于**"画布内非包含型重叠 = 0"**，两轮相同。

**（c）新出现的越界形状（相对模板）**（实测，按形状名逐页比对模板对应页）
```
P11 直线箭头连接符 27  NEW-POS  L=287.60 T=-144.00 W=0.80 H=43.14   ← 唯一新增越界
画布外形状总数 = 28 （模板继承 27 + 新增 1）
```
- 唯一新增越界形状是 `直线箭头连接符 27`（模板中 T=108.70pt 在画布内，本轮移到 T=-2in）。这是 **SPEC 第 3 轮 F1 明确授权**的"把残留连接线移出画布（设 top=-2in，不删除）"。
- 其余 27 个越界形状均为模板继承（上一版相同）。
→ **新增 1 个，且属有意授权动作，不构成新缺陷。**

---

## 3. 内容未被误伤（对照"上一轮记录的同一页文本"）

> **口径说明（重要）**：任务要求与"上一轮报告（`subagent_02.md`）记录的同一页文本"比对。**实测核查：`subagent_02.md` 并未逐页登记全文**（它是渲染/排版报告，只含坐标与片段引用）。为**独立、可复现**地重建"上一版文本"，我采用**两套互相独立的基线**并交叉验证：
> - **基线A（pptx 层）**：`build/dump_before_fix3.txt`（制作方对上一版 pptx 的形状/单元格逐条转储）。我**先独立验证了该转储方法对终版忠实**：把 `dump_after_fix4.txt` 与"我用 python-pptx 递归(含组合)读出的当前 pptx 文本 + 备注"逐页逐字比对，**完全一致**（见下）。据此基线A可信。
> - **基线B（PDF 层，完全不依赖制作方）**：我**上一轮**从 12:18 版 PDF 生成的词盒文件（`<agent-ws>/.openclaw/tmp/ppt-20260915/bbox_layout.html`）vs 本轮终版 PDF 的词盒。
>
> 另：`dump_after.txt`（第二轮产物）与 `dump_before_fix3.txt` **逐字节相同**（`diff` 计数 0），且 `subagent_02.md` 引用的文本（如 `DINO 语义分支`、P11 标签 16pt）与 `dump_before_fix3.txt` 吻合 → 佐证"上一版 = dump_before_fix3 所述"。

**基线A 忠实性交叉验证**（当前 pptx vs dump_after_fix4）：
```
=== 当前 pptx vs dump_after_fix4（逐页拼接文本比较）===
 --> 一致：dump_after_fix4 忠实反映当前 pptx（逐页拼接文本与备注）
```

**（A）pptx 层：上一版 vs 终版，逐页文本差异**
```
--- P5 文本不同  （矩形28/32/58、矩形51 三段改写）        ← 预期
--- P7 文本不同  （表格6/11 逐格改写、表格11 新增"（口径）"行）  ← 预期
--- P8 文本不同  （矩形71：主候选 split_existing_features 未通过… → 主候选未通过…）  ← 预期
--- P11 文本不同 （三条说明与底部说明改写、新增"第三步说明"框）  ← 预期
### P8 备注不同
### P10 备注不同
### P12 备注不同
（以上未列出的页 = 逐字一致）
```
→ 形状文本差异**仅在 P5/P7/P8/P11**；其余 **P1/P2/P3/P4/P6/P9/P10/P12/P13 逐字一致**。

**（B）PDF 层（独立）：上一版 PDF vs 终版 PDF，逐页文本差异**
```
P1 逐字一致   P2 逐字一致   P3 逐字一致   P4 逐字一致
P5 不同  (仅 矩形28/32/58、矩形51 相关行)
P6 逐字一致
P7 不同  (仅 表格6/表格11 相关行)
P8 不同  (仅 矩形71 相关行)
P9 逐字一致   P10 逐字一致
P11 不同 (仅 三说明/底部说明相关行)
P12 逐字一致  P13 逐字一致
```
两套独立口径**结论一致**：差异严格限于 P5/P7/P8/P11，其余 9 页逐字未变。→ **达成。**

**（C）备注差异**：上一版 vs 终版**仅 P8 / P10 / P12** 不同（P10/P12 为来源行更正，P8 为加入 `split_existing_features` 括号说明）——与任务允许范围**完全吻合**。

---

## 4. 备注与讲稿

**（a）13 页是否都有备注**（实测）
逐页备注字符数：P1 157、P2 94、P3 215、P4 80、P5 318、P6 229、P7 251、P8 325、P9 546、P10 388、P11 279、P12 272、P13 71 → **13/13 页均有备注**。

**（b）`讲稿与备注.md` vs pptx 备注**（实测，规范化空白后逐页拼接比对）
```
P1 一致(149) P2 一致(91) P3 一致(210) P4 一致(76) P5 一致(295) P6 一致(210) P7 一致(241)
P8 一致(298) P9 一致(479) P10 一致(363) P11 一致(262) P12 一致(260) P13 一致(67)
总结: 讲稿与备注.md 与 pptx 备注逐页逐字一致
```
且各页段数与 `讲稿与备注.md` 自述的"备注段数"列（3/4/4/3/4/4/5/4/5/5/5/5/2）**逐一吻合**。→ **达成。**

---

## 5. 仓库只读性

```
$ git status --short | grep -v '^??'
(none — 无已跟踪文件被改动)
$ git diff --stat
(空, exit=0)
```
- **无任何已跟踪文件被改动**。
- 全部变更均为**未跟踪的新增文件**，且集中在 `deliverables/group_meeting_20260915/`（成品 pptx/pdf + build/ 过程件）。
- 说明：`deliverables/group_meeting_20260915/` 下 `讲稿与备注.md`、`来源与证据清单.md` 因 `.gitignore` 第 16 行 `*.md` 被忽略（`git check-ignore` 印证），故不出现在 `git status`；这不影响"未改动已跟踪文件"的结论。

---

## 6. 与制作方自述的对照（不采信，仅比对）

| 制作方 `qa_fix4_report.txt` 自述 | 我的独立实测 | 是否相符 |
|---|---|---|
| PDF 13 页 / 960×540pt | 同 | ✔ 相符 |
| P11 标签单行、分隔线不穿过 | 同 | ✔ 相符 |
| P11 空白带 0.864/0.864/1.055in | 墨迹 0.867/0.867/1.060in（300dpi），1.055in（110dpi） | ✔ 相符 |
| P11 最大空白带 1.055in **★超 1.0in★** | 1.060in **>1.0in，未达成** | ✔ 相符（自述未隐瞒） |
| P11 说明基线差 +0.0333in **★超 0.03in★** | 墨迹中心 +0.030/+0.032in，同为"略超" | ✔ 相符 |
| 其余 12 页逐像素一致（除 P11） | 内容层差异仅 P5/P7/P8/P11；几何层 diff 仅 P11 | ✔ 相符 |
| fix4 只改 P11 6 个形状 | `diff_before_after_fix4` 1 个 hunk（页 11）；实测 P11 形状几何吻合 SPEC 第 4 轮 | ✔ 相符 |

> 未发现制作方**自述与实测不符**之处。其自报的两处"★超★"（空白带、基线差）经我独立复测**成立**。

---

## 7. 无法核实 / 未执行

1. **"上一轮报告记录的逐页全文"缺失**：`subagent_02.md` 未登记全文，我以两套独立基线（制作方 pre-fix 转储 + 我上一轮生成的 PDF 词盒）重建并交叉验证；**基线A 的"上一版==dump_before_fix3"是推断**（由 `dump_after==dump_before_fix3`、`subagent_02` 引用文本吻合、时间线推断），非直接字节证明（上一版 pptx 已被覆盖）。基线B（PDF 层）不依赖该推断。
2. **换渲染引擎（LibreOffice/PowerPoint）下的折行复现未做**：本节所有"折行/孤字/溢出"结论均基于**当前 Quartz 渲染的实测**；换引擎的余量风险只能给"估算"。
3. **审美质量未评**：本报告只做几何/文本/像素层面判断，不评价配色与版式美观。
4. **人眼/多模态视觉复核未做**：以 300dpi 像素墨迹/连通域分析替代。

---

## 8. 复现命令与脚本

工作目录（仓库外）：
```
W=/Users/mac/.openclaw-autoclaw/agents/auto-coder/workspace/.openclaw/tmp/ppt-20260915-fixv
P=/Users/mac/Documents/paper/symEOOD/deliverables/group_meeting_20260915/组会汇报_20260915.pdf
X=/Users/mac/Documents/paper/symEOOD/deliverables/group_meeting_20260915/组会汇报_20260915.pptx
export PATH=/opt/homebrew/bin:$PATH
```

```bash
# 0 基础信息
pdfinfo "$P" | egrep "Pages|Page size|Producer|CreationDate"
shasum -a 256 "$P" "$X" .../build/template_source.pptx
python3 -c "from pptx import Presentation as R;p=R('$X');print(len(p.slides),p.slide_width/12700,p.slide_height/12700)"

# 1 词/行包围盒
pdftotext -bbox-layout "$P" $W/bbox_layout.html
pdftotext -bbox        "$P" $W/bbox.html
python3 $W/ex.py ; python3 $W/bbox.py $W/bbox_layout.html $W/pages.json

# 2 形状几何(含模板)
python3 $W/shapes.py "$X" $W/cur_shapes.json
python3 $W/shapes.py .../build/template_source.pptx $W/tpl_shapes.json
python3 $W/verify.py            # 越页词盒/画布外形状/重叠/孤字行/行首标点/单行
python3 $W/verify2.py           # 重叠归因 + 孤字行精确判定

# 3 像素墨迹/空白带
pdftoppm -r 110 -png -f 11 -l 11 "$P" $W/png110/p11
pdftoppm -r 300 -png -f 7  -l 7  "$P" $W/png300/p7
pdftoppm -r 300 -png -f 11 -l 11 "$P" $W/png300/p11
python3 $W/pix.py   $W/png110/p11-11.png                     # 全页空白带
python3 $W/probe.py $W/png110/p11-11.png bands 330 540       # 墨迹带 x 范围
python3 $W/probe.py $W/png110/p11-11.png rect 285 76 292 200 # 箭头区域墨迹=0

# 4 内容/备注比对
python3 $W/cmp_dump.py .../build/dump_before_fix3.txt .../build/dump_after_fix4.txt
python3 $W/check_dump_vs_pptx.py .../build/dump_after_fix4.txt     # dump 忠实性
# （另：上一版 PDF 词盒 vs 终版 PDF 词盒，见 §3(B)）

# 5 仓库
cd /Users/mac/Documents/paper/symEOOD && git status --short && git diff --stat
```

脚本清单（均在我工作区 `$W`，仓库外）：

| 脚本 | 作用 |
|---|---|
| `ex.py` / `ppttext.py` | 逐页导出 pptx 文本与备注（后者递归组合形状） |
| `bbox.py` | 解析 `pdftotext -bbox-layout` → 逐页词/行盒 JSON |
| `shapes.py` | 转储形状几何（EMU/pt）+ 文本（含表格） |
| `verify.py` / `verify2.py` | 越页词盒、画布外形状、两两重叠归因、孤字行、行首标点、单行 |
| `pix.py` / `probe.py` | PNG 空白带 / 区域墨迹 / 墨迹带 x 范围 |
| `cmp_dump.py` | 解析并逐页比对两版 dump（文本+备注） |
| `check_dump_vs_pptx.py` | 验证 dump 是否忠实反映当前 pptx |

---

## 9. 给主线程的建议（仅事实，取舍由仲裁）

1. **P11「行3→底部说明」空白带 1.06in > 1.0in**：若要严格满足第 4 轮阈值，需把 `文本框 新增第三步之后`（底部说明）上移约 0.07in（或把三个计划行整体下移），使空带 ≤1.0in。属**排版收口**，不影响内容正确性。
2. **P11 说明/标签中心线偏差 ~0.03in（临界略超）**：若必须 ≤0.03in，把三个说明框 `top` 再上移 0.01in（或改用与标签同字号）即可，纯几何微调。
3. 其余各项（内容、孤字行、行首标点、断字、重叠、越界、备注、讲稿、仓库只读）**均已达成**，终版可作为交付件。
