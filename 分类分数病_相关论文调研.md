# 分类分数病 —— 相关论文调研报告

## 问题定义
SymEOOD 暗段 (133..171)：回归头 decoded 框 RIoU=0.58-0.84，但 cls head 打分 0.006-0.01。
几何正确但分数极低 → 导致 NMS 排序失败 → K=500 oracle 可救 MCML→4。

## 论文分类

### 一、分类-IoU 联合表示（Classification-IoU Joint）

| 论文 | 会议 | 核心思路 | SymEOOD 状态 |
|------|------|---------|-------------|
| **GFLv1** (Generalized Focal Loss) | NeurIPS 2020 | QFL: cls score 直接学 IoU 目标，消除训练/推理 quality 分支不一致 | ❌ 已试，退化为 FL + score 下漂 |
| **GFLv2** | CVPR 2021 | DGQP: 用回归分布统计量(TopK+mean)预测 IoU quality，再和 cls score 乘 | ⚠️ 有讨论价值 |
| **VFNet** (VarifocalNet) | CVPR 2021 | Varifocal Loss: 对正样本加重 IoU 权重、负样本降权的不对称 FL | 未试 |
| **IoU-aware RetinaNet** | IVC 2020 | 回归分支并行加一个 3×3 conv → sigmoid 输出预测 IoU，乘 cls score | 未试 |

### 二、排序一致性损失（Ranking-based Loss）

| 论文 | 会议 | 核心思路 | SymEOOD 状态 |
|------|------|---------|-------------|
| **Rank & Sort Loss** | ICCV 2021 (Oral) | 分类器监督目标：positives 分数 > negatives；且 positives 之间按 IoU 排序。无需额外 quality head | 未试 |
| **YOLO-AlignRank** | FCIS 2025 | RCQ-Loss: candidate set 内 softmax 对齐分类分数分布与 IoU 分布 + pairwise hinge | 未试 |

### 三、分数校准（Score Calibration）

| 论文 | 会议 | 核心思路 | SymEOOD 状态 |
|------|------|---------|-------------|
| **Multivariate Confidence Calibration** | CVPRW 2020 | 检测框置信度校准依赖位置/尺度；提出 box-sensitive 后处理校准方法 | 未试 |
| **MCCL** | arXiv 2023 | MC dropout + 辅助损失联合校准多类别置信度和定位 | 未试 |

---

## 关键问题：为什么 QFL 在 SymEOOD 上失败了？

QFL 的监督目标是连续的 IoU 值 (0~1)。在暗域下：
- 模型特征判别力不足以区分 "RIoU=0.7 的框" 和 "RIoU=0.3 的框"
- 模型无法拟合连续 IoU 目标 → 退化到二值 FL 行为（只区分正/负）
- scores 整体下漂 → 模型选择"保守"，对所有正样本都输出低分

**这意味着：暗域特征缺乏支持精细 quality 预测的判别力。在这些特征上做任何 quality-aware 损失（QFL/Varifocal/IACS）都可能遇到同样的退化。**

---

## 推荐分析

### 🥇 最有希望：Rank & Sort Loss
**理由**：
- 直接优化排序目标（"好框排前面"），而不是品质回归
- 不需要额外 quality head — 不会引入更多参数
- RS Loss = ranking error（正负分离） + sorting error（正样本内部排序）
- 排序是最终目标，RS Loss 用 loss 直接对准目标，绕过了"预测 IoU 值再排序"的中间步骤
- ICCV 2021 Oral，mmdetection 有官方实现
- **关键**：如果排序损失也能在暗域退化，那说明暗域问题对任何 loss 都不可学，需要从特征层面解决

**风险**：
- RS Loss 基于 pairwise ranking，candidate pool 大的时候计算量大
- SymEOOD 单类别（num_classes=1），RS Loss 的 ranking 设计需适配
- 若 dark 正样本特征区分度不够，sorting error 也可能学不好

### 🥈 值得一试：GFLv2 的 DGQP（分布引导质量预测）
**理由**：
- 核心洞察：回归分布的"尖锐程度"与定位质量高度相关
- 暗段回归头是工作的（RIoU 0.58-0.84），分布统计量应能反映框质量
- DGQP 是一个极轻量的 MLP，从回归分布中提取特征预测 IoU，**不碰 cls head**
- 推理时：score_final = cls_score × predicted_IoU_from_DGQP
- 这绕过了 cls head 的暗域退化问题

**风险**：
- 需要额外实现 GFLv1 的 General Distribution 回归方式（当前用的是 DeltaXYWHAOBBoxCoder）
- 回归头改动可能影响 A-RMSE

### 🥉 可快速验证：IoU-aware head
**理由**：
- 实现极简：回归分支最后一层并行加一个 3×3→sigmoid 预测 IoU
- 训练时 IoU head 用 L1/L2 监督 decoded box 的 IoU
- 推理时 score_final = cls_score × predicted_iou
- 改动最小、风险最低的 probe

**风险**：
- 如果暗域回归特征也不够好，IoU head 同样预测不准
- 只是"乘一个数"，不会改变相对排序。如果 dark 框的 predicted IoU 也低，乘完后依然排不上去

### 其他

- **VFNet / Varifocal Loss**：与 QFL 同类，暗域大概率同样退化，优先级低
- **MCCL 校准**：基于 MC dropout 的校准，主要用于 overconfidence 修正，SymEOOD 是 underconfidence，方向反了
- **Multivariate Calibration**：后处理校准，依赖大量验证数据，不适合 test_real 全程冻结的约束

---

## 实施建议

```
优先级：
1. IoU-aware head（最快验证，1-2天）→ 验证回归特征是否支持 quality prediction
2. Rank & Sort Loss（最有理论前景，3-5天）→ 直接优化排序目标
3. GFLv2 DGQP（最有创新性，5-7天）→ 但需要先实现 General Distribution 回归
```

**核心判据**：如果 IoU-aware head 在暗域也预测不准 IoU，说明问题出在回归特征上（与 QFL 失败的结论一致），那说明**暗域特征的根本性重建（多尺度/高分辨率/更强 FPN）是绕不开的前提**，所有分数层面的修补都是治标不治本。

---

## 2025-2026 年新论文（第二轮搜索）

### 🥇 最相关：PQA — Pixel-level Quality Assessment for Oriented Object Detection
- **发表**：arXiv 2511.08186, 2025.11
- **核心思路**：在 Rotated RetinaNet 的定位子网络加一个 3×3 conv 预测**像素级全局位置热力图 H**（2D 高斯编码像素相对 GT 框的位置）。推理时对每个预测框 b_i：
  1. 从 H 截取 b_i 区域 → H_i
  2. 用同一高斯公式编码 b_i 自身位置 → F_i
  3. 计算 Volume IoU：Q(b_i) = Σ min(H_i, F_i) / Σ max(H_i, F_i)
  4. CQ(b_i) = cls_score × Q(b_i) → 最终排序分

- **为什么特别相关**：
  - 精确针对 rotating object detection 的 quality estimation
  - 在 Rotated RetinaNet (HRSC2016) 上 **+5.96% AP50:95**
  - 即插即用：只加一个 3×3 conv + LD Loss
  - 核心洞察：框级 IoU 预测有"结构性耦合"——预测框本身由 GT 估计派生，框级 IoU 会高估低质量框；像素级空间一致性避免了此偏差
  - 有轻量版 PQA-Lite（Top-K 框选择 + 像素降采样），速度从 46.9→52.0 FPS

- **与 SymEOOD 的契合度**：
  - ✅ 同一框架（Rotated RetinaNet + mmrotate）
  - ✅ 单类别场景不需要修改分类相关逻辑
  - ✅ 实现简单（一个卷积层）
  - ⚠️ HRSC2016 上的 +5.96% 是在正常光照下，暗域像素质量可能同样退化
  - ⚠️ 需要模型能预测准确的全局热力图 H（暗域下热力图质量未知）

- **论文链接**：https://arxiv.org/abs/2511.08186

---

### 🥈 高度相关：YOLO-AlignRank — RCQ-Loss
- **发表**：FCIS (Frontiers in Computing and Intelligent Systems), 2025.08
- **作者**：Run Li, Fang Liu
- **核心思路**：RCQ-Loss (Rank-Consistent Quality Loss)
  - 对每个 GT g，设其候选集 S_g，RCQ-Loss 包含两个 term：
    1. **Soft distribution-alignment**：在 S_g 上对分类分数分布做 softmax → 与归一化 IoU 分布做 CE，强制"分数分布 ≈ IoU 分布"
    2. **Pairwise hinge**：对 S_g 中任意 (i,j)，若 IoU_i > IoU_j，则要求 score_i > score_j（hinge penalty）

- **为什么相关**：
  - 直接解决分类-定位错位（与 SymEOOD 问题本质一致）
  - ranking 层面的直接优化，不依赖 quality regression
  - 训练侧改动，推理无开销

- **与 RS Loss 的区别**：RCQ-Loss 的 pairwise hinge 更轻量，且 soft distribution-alignment term 比 RS Loss 的连续排序更柔和。在有 anchor 的单阶段检测器中可能更适配。

---

### 🥉 有参考价值：FAA — Fourier Angle Alignment (CVPR 2026)
- **发表**：CVPR 2026
- **核心思路**：用傅里叶频域估计旋转物体的主方向，做显式对齐
  - FAAFusion：FPN neck 处高频方向对齐解决"方向不一致"
  - FAA Head：RoI 特征预旋转到规范角度，解耦分类（需旋转不变性）与回归（需旋转敏感性）
- **为什么不直接相关**：
  - 解决的是 OBB 的**角度回归精度**问题（task conflict），不是 scoring 问题
  - 但 insight 对理解 OBB 检测的任务冲突有参考价值

---

### 其他 2025-2026 相关论文

| 论文 | 时间 | 核心 | 相关性 |
|------|------|------|--------|
| **SAQC** (Sensors 2026.07) | 2026 | BEV LiDAR 检测的 score calibration，坐标增强 local BEV patch + post-hoc sigmoid | 低（3D LiDAR 场景） |
| **Confidence Scores in OVD** (arXiv 2026.07) | 2026.07 | 分析 OVD 中 confidence 是 scale bias + semantic bias 的混合；temperature scaling 可部分修正 | 中（分析视角有价值，但 OVD 场景不同） |
| **PWOOD** (CVPR 2026) | 2026 | 部分弱监督 OOD（水平框/点标注训练旋转检测器） | 低（弱监督范式） |
| **夜间域适应检测** (计算机应用 2026) | 2026 | 光度对齐 day→night domain adaptation | 低（域适应范式） |
| **DAMO-YOLO 低光照置信度调优** (CSDN 2025) | 2025 | 实测分析低光照下置信度失真原因（信噪比↓→分类置信度系统性压低） | 中（现象分析与我们一致） |

---

## 新增综合推荐（2025-2026 搜索后）

### PQA 已成为 🥇 首选

**理由汇聚**：
1. **同一检测框架**：PQA 在 Rotated RetinaNet + mmrotate 上验证，代码可直接参考
2. **最简单的改动**：一个 3×3 conv + LD Loss，不影响锚框/回归/分类头的现有逻辑
3. **已有 OBB 实证**：HRSC2016 +5.96% AP50:95，DOTA +2.39% AP50:95
4. **理论匹配**：像素级空间一致性评估绕过了框级 IoU 预测的耦合偏差，这是暗域下更鲁棒的质量评估方式
5. **风险可控**：轻量版 PQA-Lite 可保证 infer speed 不退化

### RCQ-Loss 可作为 loss 层面补充

如果 PQA 在暗域下热力图预测也不准（pixel-level 退化），RCQ-Loss 提供了一个不需要显式 quality regression 的方案。可与 PQA 正交组合。

### 修正后的实施优先级

```
1. PQA (1-2天) → 即插即用，已有 Rotated RetinaNet OBB 验证
2. RCQ-Loss (2-3天) → loss 层面直接优化排序，与 PQA 正交
3. RS Loss (3-5天) → 如 RCQ-Loss 效果不足再试
4. GFLv2 DGQP (5-7天) → 需要 General Distribution 回归前置
```

**关键判据不变**：如果 PQA 的全局热力图 H 在暗域也退化了（预测不准），说明暗域问题对"质量评估 proxy"也是系统性退化——那不是质量评估方法不对，而是**暗域特征本身不支持区分性质量评估**，必须回到特征重建路径。
