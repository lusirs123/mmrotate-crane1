# 代理暗化数据与冻结残差适配器 —— 可借鉴论文调研

> 目的：针对当前裁决的两大下一步——(1) 构造"几何保留 + 分类崩塌"的合格暗域代理数据；(2) 通过零初始化分类残差头在冻结 BrightAug 上压制背景错误峰——检索可借鉴论文，并标注每篇与本项目约束的契合点与不可直接照搬之处。
> 调研日期：2026-07-20
> 与已有文档关系：本文不重复 `分类分数病_相关论文调研.md`（QFL/GFL/Varifocal/RS Loss/PQA/RCQ-Loss）和 `文献调研_模型问题解法.md`（TOOD/AMROD CTTA/STTA）中已覆盖的论文，只补充两条新路线所需的新文献。

---

## 0. 问题再聚焦（一句话映射）

| 本项目当前病灶 | 需要借鉴的论文方向 |
|---|---|
| 真实低光下前景分类特征失去判别力，画面内工业背景结构（设备边缘、栏杆、灯光、文字、金属反射）形成高分错误峰 | (A) 低光"物体-背景欺骗"建模；(B) 结构化背景干扰的数据合成；(C) 背景感知的假阳抑制 |
| 现有两种合成暗化复现不了深层排名崩塌（真实验证视频沉默比例仅 8.6%，正确候选仍排第 1 名） | (D) 失败模式定向复现的代理数据生成；(E) 上下文一致的 copy-paste 背景 |
| 旧 `AuxDetachClsHead` 用最高值融合只能抬分不能压错误峰 | (F) 零初始化残差适配器；(G) 有界 logit 残差/重评分；(H) 在线 top-K 假阳挖掘 |

---

## 1. 方向 A：低光"物体-背景欺骗"建模 —— 最直接的现象对照

### 1.1 YES — You should Examine Suspect cues for low-light object detection
- **出处**：Journal of Visual Communication and Image Representation, 2024
- **链接**：https://www.sciencedirect.com/science/article/pii/S1077314224003527 ｜ 代码：https://github.com/Regina971/YES
- **核心贡献**：首次显式定义 **"Low-Light Object-Background Cheating"** 问题——不均匀光照在物体边界形成强梯度，使模型无法判断边界来自物体本身还是光照变化；强光使物体边界弥散进背景，阴影遮蔽真实边缘，模型把背景元素误认为物体的一部分或反之。
- **方法**：OBE（光学平衡增强器：逆向色调映射 + 白平衡 + gamma 校正，平滑明暗梯度、恢复阴影细节、增强边缘连续性）+ EAM（纠缠衰减模块：Transformer 解码器分析物体与背景关系，多尺度上下文整合，准确区分物体与背景特征）。
- **与 SymEOOD 的契合点**：
  - ✅ 它定义的"物体-背景欺骗"与本项目"90.9% 第一名错误来自重复背景结构"是同一现象的学术命名，可直接引用作为问题定义。
  - ✅ EAM 的"分析物体区域与受光照变化影响的周围区域"思路，与本项目"从暗图本身的前一万候选中选择最高分错误背景作为负样本"的在线挖掘方向一致。
- **不可直接照搬**：
  - ⚠️ YES 是通用低光检测（ExDark/DarkFace），用增强+解耦改善特征；本项目已证明 BrightAug 增强路线在真实暗段仍有正确框但排名 5708，增强不是瓶颈。
  - ⚠️ EAM 依赖 Transformer 解码器，改动量大；本项目要求"只加零初始化残差头，不动主分支"。
- **借鉴价值**：**问题定义与现象命名**。可在论文中引用 YES 的"object-background cheating"概念来正式描述本项目病灶，避免自造术语。

### 1.2 ESDA — Enhanced Soft Domain Adaptation for object detection in the dark
- **出处**：Journal of Visual Communication and Image Representation, 2025 (Vol. 106, 104337)
- **链接**：https://www.scholarmate.com/S/5BUj5c
- **核心贡献**：发现环形频谱交换生成的低光图中**背景被过度对齐**是迁移效果差的根源。提出三组件：SDA（对前景/背景给不同软标签补偿背景过对齐）+ HF（Highlight Foreground，中心采样增加高质量背景样本）+ SB（Suppress Background，用背景分数替换前景分数来弱化不可迁移的背景对齐）。在 ExDark 上相对提升 11.8%。
- **与 SymEOOD 的契合点**：
  - ✅ SB（Suppress Background）直接对应本项目"残差头既要能抬目标也要能压错误峰"中的**压错误峰**方向——它证明了在暗域检测中主动压制背景对齐是有效的，而不是只增强前景。
  - ✅ 它发现"背景过度对齐反而有害"的洞察，解释了为什么本项目现有两种合成暗化（只做光度/传感器暗化）复现不了真实病灶——它们没有引入需要被压制的背景结构。
- **不可直接照搬**：
  - ⚠️ ESDA 是域适应范式（需源域+目标域配对），本项目 test_real 冻结、不能用目标域数据训练。
  - ⚠️ SB 的实现是训练时软标签替换，属于主分类目标修改；本项目已封存 QFL 这类主 target 修改路线。
- **借鉴价值**：**背景压制的设计动机**。为"残差头必须有负向（压制）能力"提供文献支撑，区别于旧 AuxDetachClsHead 的最高值融合（只能抬不能压）。

---

## 2. 方向 B：结构化背景干扰的数据合成 —— 代理数据生成

### 2.1 BIB-Copy-Paste — Background Instance-Based Copy-Paste Data Augmentation
- **出处**：Electronics (MDPI), 2023, 12(18), 3781
- **链接**：https://www.mdpi.com/2079-9292/12/18/3781
- **核心贡献**：不是随机 paste 物体，而是**为所有物体类生成背景伪标签**（计算物体背景特征与图像区域特征在欧氏空间的相似度），训练背景分类器指导 copy-paste 确保**上下文相关性**。在 PASCAL VOC 2012 上，上下文相关 paste 比随机 paste 的 mAP 提升从 0.18% 提高到 1.19%。
- **与 SymEOOD 的契合点**：
  - ✅ 直接对应本项目"来源域背景块干扰"方向——从训练数据非目标区域提取金属结构/灯光/边缘纹理，paste 到其他训练图背景中。
  - ✅ 它证明了"上下文相关的背景 paste"远优于"随机 paste"，这支持本项目要求"背景块来源必须是训练数据中的真实工业结构，不能是随机噪声"。
- **不可直接照搬**：
  - ⚠️ BIB-Copy-Paste 的背景分类器是为"paste 物体到合适背景"设计的；本项目需要的是反方向——"paste 背景干扰到目标周围"，需要重新定义上下文相关性判据。
  - ⚠️ 它不处理时间连续性；本项目要求"连续视频帧中缓慢变化"。
- **借鉴价值**：**背景 paste 的上下文一致性判据**。可借鉴其"背景特征相似度计算"来确保 paste 的工业背景块在纹理/尺度上与宿主图一致，避免引入不现实的合成痕迹。

### 2.2 Depth-Copy-Paste — Multimodal and Depth-Aware Compositing
- **出处**：arXiv 2512.11683, 2025
- **链接**：https://www.emergentmind.com/papers/2512.11683
- **核心贡献**：用 BLIP+CLIP 做语义+视觉一致性背景检索，SAM3+Depth-Anything 做遮挡感知的前景提取，深度引导的滑窗放置确保几何连续性。在 WIDER Face Hard 子集上比随机 copy-paste 提升 1.3 mAP。
- **与 SymEOOD 的契合点**：
  - ✅ 它强调的"几何一致性放置"（深度方差+平滑度惩罚）可直接借鉴到本项目"程序化工业边缘干扰"——生成的栏杆/眩光/金属高光的位置必须满足深度平滑约束，否则模型会学到合成痕迹而非真实背景干扰。
  - ✅ "遮挡感知提取"对应本项目"只从训练数据非目标区域提取"的约束。
- **不可直接照搬**：
  - ⚠️ 依赖 BLIP/CLIP/SAM3/Depth-Anything 等大模型，本项目是单类 OBB 工业检测，不需要语义级背景检索。
  - ⚠️ 深度图在港口起重机场景的可靠性未验证。
- **借鉴价值**：**放置位置的几何一致性约束**。可简化为"paste 位置的局部亮度方差/梯度平滑度"判据，确保干扰块不破坏画面整体结构。

### 2.3 SGN — Stochastic Guidance Network for sim-to-real generalization
- **出处**：Neurocomputing, 2025 (Vol. 657, 131468)
- **链接**：https://www.sciencedirect.com/science/article/pii/S092523122502140x ｜ 代码：https://github.com/leo-lab-511/SGN
- **核心贡献**：针对合成→真实的域差距，提出多风格扰动模块（multi-style perturbation）。关键发现：当源域与目标域特征协方差矩阵差异超过 0.35 阈值时，模型性能"断崖式下降"（mIoU 衰减高达 42.7%）。
- **与 SymEOOD 的契合点**：
  - ✅ 它的"协方差差异阈值"概念可直接用于**代理数据合格性判据**——本项目要求"沉默比例接近真实暗段（不低于 79%）"，可以用合成暗化与真实暗段的特征分布协方差差异来量化代理质量，而不仅看沉默比例。
  - ✅ 多风格扰动模块的思路对应本项目"两类相互独立的结构化背景干扰"（程序化边缘 + 来源域背景块），验证了多正交扰动优于单一扰动。
- **不可直接照搬**：
  - ⚠️ SGN 是分割任务的 sim-to-real 泛化；本项目是检测任务的代理数据生成，目标不是泛化到真实暗段而是**复现真实暗段的失败模式**。
  - ⚠️ 它的域泛化目标是"缩小域差距"，本项目的代理数据目标是"制造与真实暗段同质的分类崩塌"，方向不同。
- **借鉴价值**：**代理数据合格性的量化判据**。用特征分布差异（协方差/KL 散度）来度量合成暗化是否"足够接近"真实暗段的失败模式，比单纯比较沉默比例更严谨。

### 2.4 DiffDet4SAR — 扩散模型 + 散射特征增强抑制背景杂波
- **出处**：IEEE GRSL 2024（ESI 高被引）
- **链接**：https://damodev.csdn.net/6a43624d10ee7a33f284a1f0.html
- **核心贡献**：SAR 图像中飞机目标受离散散射点 + 严重背景杂波干扰。SFE（散射特征增强）模块在推理时降低背景杂波强度、增强目标显著性。DiffDet4SAR 把检测映射为边界框去噪扩散过程，比 SOTA 高 6% mAP50。
- **与 SymEOOD 的契合点**：
  - ✅ SAR 的"离散散射点 + 背景杂波"与本项目"工业设备边缘/栏杆/金属反射形成的高分错误峰"在结构上高度相似——都是**特定背景结构产生强响应压过目标**。
  - ✅ SFE 的"降低背景杂波强度"对应本项目残差头的"压错误峰"功能。
- **不可直接照搬**：
  - ⚠️ 扩散模型检测器架构与本项目 Rotated RetinaNet 差异极大。
  - ⚠️ SFE 针对 SAR 散射特性设计，不适用于光学图像。
- **借鉴价值**：**背景杂波抑制的学术对照**。SAR 检测领域对"背景结构产生强响应"这一问题有成熟的问题定义和评估方法，可为本项目提供跨领域的理论支撑。

### 2.5 MIGNet — Morphology Information-Guided Detection Network
- **出处**：Engineering Applications of Artificial Intelligence, 2025 (Vol. 158, 111351)
- **链接**：https://www.sciencedirect.com/science/article/pii/S0952197625013533
- **核心贡献**：针对遥感图像中复杂背景杂波（噪声 + 伪目标），提出标签分配联合分类与回归分数——只有两者都高时才给 anchor 高正样本权重，有效减少伪目标干扰。频域分析抑制低频背景噪声、增强高频目标轮廓。
- **与 SymEOOD 的契合点**：
  - ✅ "伪目标"概念直接对应本项目的"背景结构产生高分错误峰"。MIGNet 的洞察是：增强目标轮廓的同时也会放大伪目标的分类信号，因此必须在标签分配层联合判别——这与本项目"不能只靠增强，必须有独立的分类判别/压制"的结论一致。
  - ✅ 它的"只有分类+回归都高才给正权重"的分配策略，为残差头的训练目标设计提供了参考——残差头应学"分类+几何联合判别"，而不是只学分类。
- **不可直接照搬**：
  - ⚠️ MIGNet 修改的是标签分配策略（训练侧），本项目已封存主分配器/主 target 修改路线。
  - ⚠️ 频域分析模块改动量大。
- **借鉴价值**：**伪目标问题的标签分配视角**。为"残差头的正/负样本定义"提供参考——正样本=正确候选（分类高+几何好），负样本=高分错误背景（分类高+几何差），而非简单的前景/背景二分。

---

## 3. 方向 C：背景感知的假阳抑制 —— 在线 top-K 挖掘

### 3.1 BEM — Background Embedding Memory for False-Positive Suppression
- **出处**：2025（固定摄像头监控场景）
- **链接**：https://www.themoonlight.io/zh/review/bem-training-free-background-embedding-memory-for-false-positive-suppression-in-real-time-fixed-background-camera
- **核心贡献**：训练自由的推理时模块，附加到冻结检测器上。维护背景原型嵌入 $E_B$，计算当前帧与背景的余弦相似度 $c$，在 logit 空间做相似度感知重评分：
$$z'_i = \text{logit}(\tilde{s}_i) - \alpha \frac{\gamma}{w_i \cdot \max(c, \delta)}$$
其中 $w_i = \frac{N-r_i}{N+1}$ 是排名加权（对高置信提议施加更小惩罚），$\alpha,\gamma$ 控制惩罚幅度。低背景相似度→更强惩罚。
- **与 SymEOOD 的契合点**：
  - ✅ **这是与本项目"零初始化分类残差头"架构最接近的已发表方法**。它在冻结检测器上做 logit 空间的有界加性修正，既能抬分（高相似度时惩罚小）也能压分（低相似度时惩罚大），正好满足"残差既要能抬目标也要能压错误峰"。
  - ✅ 排名加权 $w_i$ 的设计——对高置信提议少惩罚、对低置信提议多惩罚——可借鉴到本项目"从前一万候选中选最高分错误背景作为负样本"的在线挖掘策略。
  - ✅ 固定摄像头场景与本项目港口固定机位起重机监控场景一致。
- **不可直接照搬**：
  - ⚠️ BEM 是训练自由的（用背景帧时序聚合），本项目需要的是可训练的残差头（能从代理数据学习判别力）。
  - ⚠️ BEM 的背景原型来自"无目标的干净背景帧"，本项目暗段中目标始终存在，无法获得干净背景帧。
- **借鉴价值**：**有界 logit 残差的数学形式与排名加权设计**。BEM 的公式直接证明了"在冻结检测器 logit 上加有界修正项"是可行且有效的范式，为本项目的残差头 `final_score = main_cls_score + bounded_residual` 提供了文献先例。

### 3.2 AFPS — Actionness-guided False Positive Suppression
- **出处**：Neural Networks, 2024
- **链接**：https://www.sciencedirect.com/science/article/abs/pii/S0893608024002314
- **核心贡献**：弱监督时序动作定位中，用自训练 actionness 分支（独立于分类分支）估计每个片段的假阳概率，对"高分类分数 + 高非动作分数"的片段用类均匀标签最大化熵来抑制。FGE（前景增强）模块通过注意力增强前景权重。
- **与 SymEOOD 的契合点**：
  - ✅ "独立于分类分支的自训练 actionness"对应本项目"独立于主分类的残差头"——不碰主分类目标，用独立分支判别前景/背景。
  - ✅ "高分类分数 + 高非动作分数 = 假阳"的判别逻辑，直接映射到本项目"从前一万候选中选分类高分但几何错误的背景作为负样本"。
  - ✅ 用熵最大化抑制假阳的思路，可作为残差头训练损失的设计参考——不只是让负样本分数低，而是让其在所有类别上均匀分布（最大熵=最无判别力）。
- **不可直接照搬**：
  - ⚠️ AFPS 是时序动作定位（视频片段级），本项目是空间检测（候选框级）。
  - ⚠️ 自训练依赖伪标签，本项目代理数据有标注。
- **借鉴价值**：**独立分支 + 假阳概率估计 + 熵抑制**的三段式设计。为残差头的训练目标提供"不只是二分类压制，而是让错误峰在分数空间均匀化"的思路。

### 3.3 RF-DETR 的 Hard Negative Mining
- **出处**：arXiv 2504.13099 / 2509.02599, 2025-2026
- **链接**：https://www.emergentmind.com/topics/rf-detr
- **核心贡献**：DETR 式检测器中引入显式 hard negative mining 损失项 $\mathcal{L}_{\text{hnm}}$，惩罚过度自信的假阳性。在组织病理学跨域场景中，用"包含混淆因子（坏死、炎症）的组织批次"作为挖掘的负样本，减少过度自信假阳。
- **与 SymEOOD 的契合点**：
  - ✅ "惩罚过度自信的假阳性"正是本项目"压错误峰"的需求。$\mathcal{L}_{\text{hnm}}$ 作为独立损失项加入总损失，不修改主分类损失——与本项目的残差头独立于主分类一致。
  - ✅ "包含混淆因子的负样本"对应本项目"从前一万候选中选最高分错误背景"——不是随机负样本，而是模型最容易混淆的背景结构。
- **不可直接照搬**：
  - ⚠️ RF-DETR 是 DETR 架构（query-based），本项目是 anchor-based RetinaNet。
  - ⚠️ $\mathcal{L}_{\text{hnm}}$ 的实现依赖 set prediction 的 Hungarian 匹配。
- **借鉴价值**：**显式 hard negative mining 损失项的工程实现**。可将 $\mathcal{L}_{\text{hnm}}$ 的思想移植到 anchor-based 框架：对前一万候选中分类高分但 RIoU<0.5 的候选施加额外惩罚。

---

## 4. 方向 D：零初始化残差适配器 —— 冻结主分支 + 有界修正

### 4.1 AdapterTune — Zero-Initialized Low-Rank Adapters for Frozen Vision Transformers
- **出处**：arXiv 2603.14706, 2026
- **链接**：https://huggingface.co/papers/2603.14706 ｜ 代码：https://github.com/salimkhazem/adaptertune
- **核心贡献**：针对冻结 ViT 迁移学习的两个问题（优化不稳定、容量选择无指导），在每个 transformer block 加残差低秩瓶颈，**上投影零初始化**——保证适配后网络在训练起点精确等于预训练函数，消除早期 epoch 的表征漂移。在 5 数据集迁移套件上比 head-only 迁移平均提升 14.9 个点，仅需全微调 0.92% 的参数。形式化 adapter rank 为"近似下游任务特征空间偏移的容量预算"，预测单调但递减的精度增益。
- **与 SymEOOD 的契合点**：
  - ✅ **这是本项目"零初始化分类残差头"最直接的理论与工程先例**。AdapterTune 明确证明：零初始化的上投影保证 `f_adapted(x) = f_pretrained(x)` 在 step 0 成立，训练从预训练函数出发而非随机扰动——这正是本项目要求"冻结 BrightAug epoch20 主分支 + 零初始化残差"的核心动机。
  - ✅ "容量预算"理论可指导残差头的设计——残差头的 rank/通道数应作为"近似暗域分类偏移的容量预算"，太小拟合不了，太大破坏预训练。
  - ✅ "消除早期 epoch 表征漂移"直接对应本项目"正常亮度第一名命中率下降不超过 1%"的通过条件——零初始化保证训练初期不影响亮域精度。
- **不可直接照搬**：
  - ⚠️ AdapterTune 针对 ViT（transformer block），本项目是 CNN（ResNet+FPN+RetinaHead）。残差低秩瓶颈需要适配到卷积分类头。
  - ⚠️ 它是分类任务的迁移学习，本项目是检测任务的域适配，需要同时考虑候选级而非图像级。
- **借鉴价值**：**零初始化的理论保证与容量预算设计**。为本项目残差头的初始化策略（上投影层权重初始化为 0）、容量选择（rank/通道数扫描）、通过条件（亮域精度不退化）提供完整的文献支撑。

### 4.2 Repair — Residual Decomposition Framework for Long-Tailed Reranking
- **出处**：arXiv 2604.01506, 2026
- **链接**：https://arxiv.org/html/2604.01506v1 ｜ 代码：https://github.com/WGLab/REPAIR
- **核心贡献**：研究 base model top-k shortlist 上的 Bayes 最优重排序。把"最优分数与 base 分数之差"（残差修正）分解为：**类间分量**（classwise，每个类内恒定）+ **成对分量**（pairwise，依赖输入和竞争标签）。证明了当同一标签对在不同上下文中产生不兼容的排序约束时，固定类间偏移无法恢复。提出 Repair：收缩稳定的类间项 + 由竞争特征驱动的线性成对项。
- **与 SymEOOD 的契合点**：
  - ✅ **残差分解理论直接指导本项目残差头的设计**。本项目的场景是：同一正确候选在亮域排第 1、暗域排第 5708——这正是 Repair 所说的"同一标签对在不同上下文中产生不兼容排序约束"，固定偏移无法解决，需要依赖输入的成对修正。
  - ✅ "由竞争特征驱动的线性成对项"对应本项目"从前一万候选中选最高分错误背景作为负样本"——残差不是对单个候选独立打分，而是考虑候选之间的竞争关系。
  - ✅ top-k shortlist 重排序的范式，与本项目"保留前一万候选、在阈值前重排"完全一致。
- **不可直接照搬**：
  - ⚠️ Repair 是后处理重排序器（post-hoc），本项目需要可训练的残差头。
  - ⚠️ 长尾分类的类间分量在本项目单类检测中不直接适用。
- **借鉴价值**：**残差修正需要输入相关的成对项，不能只有全局偏移**。为本项目残差头"必须从候选特征生成、并考虑候选间竞争关系"提供理论依据——残差不是对单个候选独立打分，而是要在前一万候选的竞争上下文中判别"正确框"与"高分错误背景"的相对关系。这正是新裁决中"从前一万候选中在线选最高分错误背景作为负样本"这一环节的理论支撑：负样本选择本身就是构造竞争上下文，让残差学到输入相关的成对修正，而非全局偏移。

### 4.3 Logit Adjustment — 经典有界 logit 修正
- **出处**：Menon et al., 2020（长尾分类经典）
- **综述**：https://www.emergentmind.com/topics/logit-adjustment
- **核心公式**：$\tilde{z}_j = z_j + \tau \ln \pi_j$，加性修正项 $\tau \ln \pi_j$ 是有界的类先验对数。
- **与 SymEOOD 的契合点**：
  - ✅ 提供了"在 logit 上加有界修正项"的经典数学形式。本项目的 `final_score = main_cls_score + bounded_residual` 中，`bounded_residual` 可以用 tanh 或 clip 保证有界，形式上与 logit adjustment 同构。
  - ✅ 它的训练时 vs 推理时两种使用方式，对应本项目残差头的训练时（学残差）和推理时（应用残差重排）两个阶段。
- **不可直接照搬**：
  - ⚠️ Logit Adjustment 的修正项是类先验的函数（长尾场景），本项目的残差应是候选特征的函数（暗域判别）。
- **借鉴价值**：**有界 logit 修正的数学基础**。确保残差头输出的修正项有界（如 tanh 限幅到 [-α, α]），不会无限制地抬高或压低分数，保护亮域精度。

### 4.4 SEEN-DA — Semantic Entropy Guided Domain-Aware Attention（CVPR 2025）
- **出处**：CVPR 2025, pp. 25465-25475
- **链接**：https://is.cas.cn/ztzl2016/2025xsnh/205hbzs/202508/P020250916680966936771.pdf
- **核心贡献**：在**冻结的 VLM 视觉编码器**上加轻量可学习的域感知注意力模块（域间共享 + 域内独有），用语义熵估计视觉特征重要性，消除语义无关冗余信息、激活域不变特征并补充域特定特征。
- **与 SymEOOD 的契合点**：
  - ✅ "冻结主干 + 轻量可学习注意力模块"的架构范式与本项目"冻结 BrightAug + 零初始化残差头"一致。SEEN-DA 证明了在冻结检测器上加轻量可学习模块做域适配是 CVPR 级的可行方案。
  - ✅ "域间共享 + 域内独有"的双分支设计可借鉴——残差头可以有共享部分（亮暗通用判别）和域特定部分（暗域专用压制）。
- **不可直接照搬**：
  - ⚠️ SEEN-DA 依赖 VLM（文本嵌入语义熵），本项目是纯视觉单类检测。
  - ⚠️ 它是无监督域适应（需目标域无标注数据），本项目 test_real 冻结。
- **借鉴价值**：**冻结主干 + 轻量可学习适配模块的 CVPR 级先例**。为本项目的整体架构设计（冻结+适配）提供顶会文献支撑。

---

## 5. 综合推荐与映射表

### 5.1 按本项目两大需求的优先级

| 优先级 | 论文 | 方向 | 核心借鉴点 | 本项目对应环节 |
|---:|---|---|---|---|
| 🥇 | **AdapterTune** (2026) | D-零初始化残差 | 零初始化上投影保证起点=预训练函数；容量预算理论 | 残差头初始化策略与 rank 选择 |
| 🥇 | **BEM** (2025) | C-假阳抑制 | logit 空间有界加性修正 + 排名加权；冻结检测器上推理时重评分 | 残差头的数学形式与通过条件 |
| 🥇 | **YES** (2024) | A-问题定义 | "Low-Light Object-Background Cheating" 正式命名 | 论文问题定义章节 |
| 🥈 | **Repair** (2026) | D-残差分解 | 残差=类间+成对；固定偏移不够，需输入相关成对项 | 残差头必须从候选特征生成 |
| 🥈 | **ESDA-SB** (2025) | A-背景压制 | Suppress Background 证明主动压背景对齐有效 | 残差头负向能力的设计动机 |
| 🥈 | **BIB-Copy-Paste** (2023) | B-背景 paste | 上下文相关 paste 远优于随机 paste | 代理数据背景块一致性判据 |
| 🥉 | **AFPS** (2024) | C-假阳抑制 | 独立分支 + 假阳概率 + 熵抑制 | 残差头训练损失设计 |
| 🥉 | **RF-DETR HNM** (2025) | C-假阳挖掘 | 显式 hard negative mining 损失项 | top-K 候选在线负样本挖掘 |
| 🥉 | **MIGNet** (2025) | B-伪目标 | 标签分配联合分类+回归；伪目标问题 | 残差头正/负样本定义 |
| 🥉 | **SGN** (2025) | B-代理质量 | 协方差差异阈值量化域差距 | 代理数据合格性量化判据 |
| 参考 | **Depth-Copy-Paste** (2025) | B-放置约束 | 深度引导几何一致性放置 | 干扰块位置约束 |
| 参考 | **DiffDet4SAR** (2024) | B-背景杂波 | SAR 散射+杂波抑制的跨领域对照 | 背景结构强响应的问题对照 |
| 参考 | **SEEN-DA** (CVPR 2025) | D-冻结适配 | 冻结 VLM + 轻量可学习注意力 | 冻结+适配的顶会先例 |
| 参考 | **Logit Adjustment** (2020) | D-有界修正 | 经典有界 logit 修正数学形式 | 残差有界性保证 |

### 5.2 按论文与本项目的架构兼容性

| 论文 | 冻结主分支 | 不改主 cls target | 单类 OBB 兼容 | test_real 冻结兼容 | 直接攻击背景错误峰 |
|---|:---:|:---:|:---:|:---:|:---:|
| AdapterTune | ✅ | ✅ | ⚠️(ViT→CNN) | ✅ | 间接(提供架构) |
| BEM | ✅ | ✅ | ✅ | ✅ | ✅ |
| YES | ❌(改主干) | ⚠️ | ✅ | ✅ | ✅ |
| ESDA-SB | ❌(域适应) | ❌(软标签) | ✅ | ❌(需目标域) | ✅ |
| Repair | ✅(后处理) | ✅ | ⚠️(多类→单类) | ✅ | ✅ |
| AFPS | ✅(独立分支) | ✅ | ✅ | ✅ | ✅ |
| BIB-Copy-Paste | N/A(数据层) | ✅ | ✅ | ✅ | 间接(造数据) |
| MIGNet | ❌(改分配) | ❌ | ✅ | ✅ | ✅ |

---

## 6. 关键设计建议（基于文献综合）

### 6.1 代理数据生成的合格性判据（综合 SGN + YES）

不应只看"沉默比例"和"正确候选排名中位数"，应增加：
- **特征分布差异**：合成暗化与真实暗段在 FPN 特征空间的协方差差异/KL 散度（借鉴 SGN 的 0.35 阈值思路）。
- **物体-背景欺骗复现度**：合成暗化是否在目标边界产生与真实暗段类似的梯度模式（借鉴 YES 的 OBE 分析方法）。
- **错误峰归因一致性**：合成暗化的第一名错误是否也来自背景结构（设备边缘/栏杆/灯光/文字/金属反射），而非随机位置。

### 6.2 残差头的数学形式（综合 BEM + Logit Adjustment + Repair）

```
final_score = sigmoid(logit(main_cls_score) + r)
```
其中残差 `r` 应满足：
- **有界**：`r = α · tanh(w^T f_candidate)`，α 控制最大修正幅度（借鉴 Logit Adjustment 的有界性 + BEM 的 α 参数）。
- **输入相关**：`f_candidate` 是候选的 FPN 特征（借鉴 Repair 的"成对项必须输入相关"）。
- **零初始化**：`w` 初始化为 0，保证 step 0 时 `r=0`，`final_score = main_cls_score`（借鉴 AdapterTune 的零初始化上投影）。
- **可正可负**：tanh 输出 [-1, 1]，乘以 α 后可正可负，既能抬目标也能压错误峰（区别于旧 AuxDetachClsHead 的 max-fusion 只能抬）。

### 6.3 在线 hard negative mining（综合 AFPS + RF-DETR HNM）

- **负样本来源**：每帧前一万候选中，分类分数 top-100 但 RIoU<0.5 的候选作为 hard negative（借鉴 RF-DETR HNM 的"过度自信假阳"惩罚）。
- **正样本来源**：RIoU≥0.5 的正确候选。
- **损失设计**：不只是二分类 BCE，可加入熵最大化项让错误峰在分数空间均匀化（借鉴 AFPS 的 FPS 模块）。

### 6.4 通过条件的文献依据

本项目设定的通过条件在文献中都有对应支撑：
- "正常亮度第一名命中率下降不超过 1%" ← AdapterTune 证明零初始化保证起点不漂移。
- "正确候选排名中位数至少改善 30%" ← Repair 的成对残差理论预测输入相关修正比固定偏移更有效。
- "最长连续错误段至少缩短 30%" ← BEM 的排名加权设计优先压制低置信假阳。
- "两种干扰方式双向训练/验证都有效" ← SGN 的多正交扰动优于单一扰动。

---

## 7. 不建议借鉴的论文类型（避免重蹈覆辙）

以下方向虽然论文数量多，但与本项目已封存路线重合，**不应再投入时间**：

| 论文类型 | 代表 | 不借鉴原因 |
|---|---|---|
| 低光图像增强 + 检测联合训练 | Dark-YOLO, SFF-YOLO, NLE-YOLO, FDLIE-YOLO, FE-YOLO, LL-YOLO | 本项目已证 BrightAug 增强后仍有正确框但排名 5708，增强不是瓶颈 |
| QFL/Varifocal/IACS 类质量感知分类 | GFLv1/v2, VFNet, TOOD | 已封存（QFL 失败，主 target 修改路线关闭） |
| 训练侧域适应（需目标域数据） | ESDA, DA-RTDETR, SEEN-DA | test_real 冻结，不能用目标域数据训练 |
| Mean-Teacher CTTA | AMROD | 已在 `文献调研_模型问题解法.md` 评估，伪标签在暗段不可靠 |
| 后处理时序重锚 | STTA | 已在 `temporal_reanchor_probe.py` 证零训练几何规则无效 |
| PQA/RegQuality 热图质量头 | PQA v1/v2/v3 | 已封存（v1/v2/v3 均失败） |
| 零填充 | 本项目内部 | 已证只改善 10%，33 帧仍全失败，封存为诊断配置 |

---

## 8. 结论

本项目当前两大下一步的文献支撑是充足的：

1. **代理暗化数据**：YES 的"物体-背景欺骗"定义 + BIB-Copy-Paste 的上下文 paste + SGN 的代理质量量化 + MIGNet 的伪目标标签分配，共同提供了"如何构造合格代理数据"和"如何判断代理是否合格"的完整方法论。

2. **零初始化分类残差适配器**：AdapterTune 的零初始化理论 + BEM 的有界 logit 重评分 + Repair 的残差分解 + AFPS/RF-DETR 的在线假阳挖掘，共同提供了"冻结主干 + 有界残差 + 压错误峰"的完整架构与训练方案。

**最关键的三篇**：AdapterTune（零初始化架构与理论）、BEM（有界 logit 残差的工程先例）、YES（问题定义的学术命名）。这三篇覆盖了本项目新路线的核心创新点，建议优先精读。
