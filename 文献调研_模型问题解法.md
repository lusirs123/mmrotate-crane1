# SymEOOD 模型问题 — 相关解法文献调研

> 目的：针对当前模型的全部问题（几何最优但 real 域连续漏检、cls-loc 错位、domain gap），检索同类困难论文并归纳其解法，映射到本项目约束。
> 调研日期：2026-07-10

---

## 0. 问题分层（一句话回顾）

| 层 | 问题 | 本项目实证 |
|----|------|-----------|
| A. Scoring 错位 | 几何已可用（decoded RIoU 0.58–0.84），但 cls 分数排不上来（global_max≈0.006–0.01），推理排序只靠 sigmoid(cls_logit) | DEAD-global 100% 是 scoring 病 |
| B. Domain Gap | 难帧（seq02[2..41] 亮+小、seq02[129..172] 暗+大）不在训练集，训练侧任何改动都覆盖不到 | 任何 loss/feature/score 改动均失败 |
| C. 连续漏检 / 时序断裂 | O2O + SymNFL 排他性导致"宁缺毋滥"，难帧直接放弃，MCML 39–101 帧远差于 ≤5 及格线 | M2/L_equi/brightaug 均卡在 MCML=39 |

---

## 1. 问题层 A：cls-loc 错位 → Scoring 对齐解法

### 1.1 TOOD — Task-aligned One-stage Object Detection（ICCV 2021 Oral）
- 论文：https://arxiv.org/abs/2108.07755 ｜ 代码：https://github.com/fcjian/TOOD
- **核心机制 Task Alignment Learning (TAL)**：
  - **任务对齐分数** `s = p^(1-α) · t^α`，其中 `p`=分类概率，`t`=预测 IoU，α 平衡两者。
  - **Task-Aligned Sampling**：按 `s` 为每个 GT 选 top-k anchor，使"分类最优"和"定位最优"的 anchor 被拉到同一组。
  - **任务对齐损失**：回归用"概率化 GIOU 损失 ^t"（即定位质量越高的样本回归损失权重越大）；分类用 focal 类损失。
  - **T-Head** 显式学 task-interactive + task-specific 特征，且**无需单独 centerness 分支**。
- 相关性：直接对准 cls-loc 错位。若 SymEOOD 的分配器（SymPOLA）改为 TAL 式"按 cls×IoU 联合分数选 anchor"，可让几何最优 anchor 同时也是分数最高 anchor —— 从训练分配层面根治 DEAD-global。

### 1.2 VarifocalNet（CVPR 2021 Oral）— IoU 感知打分
- 论文：https://arxiv.org/abs/2008.13367 ｜ 代码：https://github.com/hyz-xmaster/VarifocalNet
- **核心机制**：
  - **IACS（IoU-aware Classification Score）**：预测"物体存在置信度 + 定位精度"的联合分数，而非纯类别概率。
  - **Varifocal Loss**：对正样本用 `y·σ(·)` 的 focal 式（关注高质量正样本），对负样本用 `(1-y)·σ(·)^γ` 的 focal 式（标准难样本挖掘）。
  - **星形框特征（star-shaped feature）**：从框中心沿 4 个方向采样，用于 IACS 预测 + 框精修。
  - 在 FCOS+ATSS 上实现，推理时**直接用 IACS 排序候选框**。
- 相关性：**这是 DEAD-global 最直接的对症药**。本项目的死段"几何对、分数错"，本质是排序用的 cls_logit 没有编码定位质量。IACS 让排序分数 = 预测 IoU，几何好的框自然排前面。

### 1.3 ICASSP 2023 — Task Spatial Misalignment（分类感知 IoU，**旋转/不规则框专属**）
- 论文：https://sigport.org/sites/default/files/docs/icassp2023_poster.pdf
- **核心机制**：
  - **classification-aware IoU 度量**：为不规则/旋转形状目标的 anchor 分配定义"分类感知的 IoU"，智能修正 anchor 标签误判（旋转框的 IoU 对角度极敏感，传统 IoU 分配易把几何好但角度偏的 anchor 误判为负/低权）。
  - **Dynamic Coefficient Loss**：同时权衡分类与定位性能的动态系数损失。
  - **Classification-Localization balanced NMS**：把定位因子引入 NMS。
- 相关性：**旋转 OBB 最贴合的一篇**。本项目 SymPOLA 用 KLD 分配，但 cls 头仍是 0/1 二值（SymNFL）——该文证明"用分类感知的 IoU 做分配 + 平衡的 NMS"能修旋转框的标签错位。对无 NMS 推理，可把"balanced NMS"改造成"balanced 排序/重锚"。

### 1.4 与本项目已做 QFL 的关系（关键警示）
- QFL 是 VarifocalNet 思想的**简化 cousin**：只把训练 target 从 0/1 改成 `exp(-kld/tau)`。它失败的根因（MEMORY 已记）：warm-start 后 quality≈0.95→退化为 FL；且**死段不在训练集，改 target 救不了 OOD 帧**。
- **VarifocalNet 与之的本质区别**：它在**推理层预测显式 IoU 感知分数并直接用于排序**（IACS），且用星形特征回归实际 IoU。它不只改训练 target，而是把"质量"变成一个可在推理时重新排序的信号。
- 但须警惕：**IACS / TOOD 仍是训练侧方法**，死段 OOD 问题不会因换一个更漂亮的损失就消失。它们最稳妥的价值是"让训练域内几何最优 anchor 的分数对齐"；要真正救 OOD 死段，必须配合测试时自适应（见 §2）。

---

## 2. 问题层 B：Domain Gap（难帧不在训练集）→ 测试时自适应

### 2.1 CTTA for Object Detection — AMROD（arXiv 2406.16439）
- 论文：https://arxiv.org/abs/2406.16439 ｜ 代码：https://github.com/ShileiCao/AMROD
- **核心机制（无需重训、无源数据）**：
  - **Mean-Teacher 自训练**：教师对弱增强测试图生成伪标签，监督学生；教师用 EMA 从学生更新（视作历史集成）。
  - **OCL 对象级对比**：RoIAlign 提对象特征，师生同提案为正对，对比学习细化目标域特征。
  - **AM 自适应监控**：按预测置信度 EMA 动态跳过不必要适配、按类更新伪标签阈值。
  - **ARR 自适应恢复**：Fisher 信息矩阵指示参数重要性，高概率重置不活跃参数，缓解灾难性遗忘。
  - **逐帧（batch=1）在线适配**，支持单帧检测器（Faster R-CNN 实验）。
- 相关性：**直接攻击 domain gap 根因**。它在测试帧本身上在线适配，不把测试数据放进训练集 —— **天然满足本项目的"test_real 全程冻结"约束**，且在线更新不改训练几何，因此**不破 A-RMSE ≤1.45 守门线**。是救 DEAD-global 死段最有希望的方向。

### 2.2 Degradation-Consistent TTA（CVPR 2026）
- 论文：https://openaccess.thecvf.com/content/CVPR2026/papers/Tang_Degradation-Consistent_Test-Time_Adaptation_for_All-in-One_Image_Restoration_CVPR_2026_paper.pdf
- **核心机制**：在无源数据/无重训下，把预训练全修复模型适配到**未见过的退化分布**（低光、模糊、噪声联合），强调退化一致性。
- 相关性：对应本项目的"暗+大目标"（seq02[129..172]）退化模式。可与 AMROD 互补 —— 退化一致性约束防止适配时破坏几何。

---

## 3. 问题层 C：连续漏检 / 时序断裂 → 时序重锚 / 轨迹关联

### 3.1 Spatial-Temporal Tracklet Association（STTA，ACM 2023）
- 论文：https://dl.acm.org/doi/10.1145/3635155
- **核心机制**：两阶段 tracklet 级关联，利用空间+时序信息把**短暂丢失的目标在后续帧重新关联**（training-free，可直接级联到现成检测器）。
- 相关性：**直接对应本项目"test-time 时序重锚"方向**（MEMORY 已识别）。DEAD-global 是几何已可用、只是分数低导致该帧被丢掉；若用相邻健康帧的几何连续性做 tracklet 关联 / 插值重锚，可在后处理层把死段"补"回来，零模型改动、零 A-RMSE 风险。

### 3.2 港口抓斗单目位姿（I2MTC 2026，领域直接对照）
- 论文：https://www.it.pt/Publications/PaperConference/41859
- **方法**：下视相机 + 抓斗 10 个结构关键点检测 + **GCN 精修模块**抑制漂移/离群 + 带已知距离与对称性约束的改进 EPnP 解算旋转角。
- 结果：关键点 MAE 1.53→0.46 px（-69.9%），角度重投影 RMSE std 3.55→0.74 px。
- 相关性：**领域最贴论文**，证明真实港口工况（低光/遮挡）需显式鲁棒处理。但其范式为 keypoint+GCN+EPnP，**不是 OBB 流水线、且引入平台/结构关键点**（违反本项目"只输出顶梁 OBB"约束），不能 drop-in。可借鉴的是 **GCN 精修 + 对称性约束抑制角度漂移**的思想，用于本项目时序平滑/重锚模块。

---

## 4. 与项目约束的兼容性矩阵

| 方向 | 训练侧改动 | 破 A-RMSE 守门线风险 | 与"test_real 冻结"兼容 | 直接攻击 OOD 死段 |
|------|-----------|----------------------|---------------------------|----------------|
| TOOD TAL / VarifocalNet IACS / ICASSP cls-aware IoU | 是（重分配/加分支） | 中（须守几何） | 是（训练不含死段） | 间接（靠域内有监督对齐） |
| CTTA / AMROD 测试时自适应 | **否**（测试时在线更新） | **低**（不改训练几何） | **是**（正是在测试帧适配） | **直接**（在死帧上适配） |
| STTA tracklet / 时序重锚（后处理） | **否**（独立后处理） | **无**（不改模型） | 是 | **直接**（补帧） |

---

## 5. 优先级建议（结合约束）

1. **最高优先 — 测试时自适应（CTTA/AMROD 轻量版）**：
   直接攻击 domain gap 根因；满足"test_real 冻结"（在测试帧在线适配，不进训练集）；不改训练几何、不破 A-RMSE 守门线。可在 SymEOOD 主头上做：伪标签自训练 + 置信度监控跳帧 + 退化一致性约束的精简实现，优先跑死段 seq02[2..41] 与 [129..172]。

2. **次优先 — 时序重锚 / tracklet 关联（STTA 思路）作为独立后处理层**：
   利用相邻健康帧几何连续性插值/重锚死段，零模型改动、零 A-RMSE 风险，正是 MEMORY 已识别的"test-time 时序重锚"方向，文献给出现成范式。先验证"几何已可用"假设下补帧能否把 MCML 39 压到合格区。

3. **训练侧再尝试须谨慎**：
   TOOD TAL / VarifocalNet IACS 是比 QFL 更完整的质量感知实现，但本质仍训练侧——QFL 已证"改训练 target 救不了 OOD 死段"。若要做，必须**在推理 ranking 层引入质量分支（IACS 式）**而非仅改 loss target，并严格守 A-RMSE ≤1.45、先在 val 验证几何不退化。ICASSP2023 的 classification-aware IoU + C-L balanced 排序是旋转 OBB 最贴、可直接嫁接到无 NMS 推理的变体。

4. **领域范式仅作借鉴**：港口抓斗 keypoint+GCN+EPnP 不能直接用（违反 OBB-only 约束），但其 GCN 精修 + 对称性约束抑制漂移的思想，可并入时序平滑/重锚模块。
