# 语义融合核对表（第二轮，2026-09-15）

本文件位于**归档之外**，是 `docs/` 三份主文档第二轮语义融合与状态纠错的核对凭据。

- 三份主文档：`冻结DINOv2与SymEOOD检测融合.md`、`OBB观测可靠性与连续输出.md`、`Webots单目深度估计.md`。
- 归档位置：`archive/20260915_pre_consolidation/`（11 份原文，逐字节未修改）。
- **归档 `manifest.json` 是 2026-09-15 首次机械整合的快照**：其中的 `anchor`、`integrated_body_sha256` 只描述当时的章节编号与结构，不对应本轮改写后的正文。它只用于原文身份与完整性核验，不构成本轮验证结果。本文件的映射才是当前有效映射。
- 本文件只做文档层面的核对；不表示重新训练、重新推理、复核权重或核验外部结果。

---

## 一、原锚点 → 新正文位置

### 1. 冻结 DINOv2 与 SymEOOD 检测融合（50 个来源章节）

| 原锚点 | 新正文位置 | 处置 |
| --- | --- | --- |
| L-1 当前结论 | 1.2 / 3.1.3 | 合并（与 L-6、D-2、D-4 重复） |
| L-2 原始问题和根因 | 3.1.1 | 保留 |
| L-3 方法设计 | 2.2 | 保留（结构 + 稳定器数学唯一处） |
| L-4 训练协议 | 2.4 / 2.5.1–2.5.2 / 5.3(a) | 拆分合并 |
| L-5 验证过程 | 3.1.2 | 合并 |
| L-6 暗光专项结果 | 3.1.3 | 合并 |
| L-7 完整 test 结果 | 3.1.4 | 合并（三块压为一表 + 口径说明） |
| L-8 DFR/ACI 退化及修复 | 3.1.5 | 合并（与 L-13 同题） |
| L-9 数据泄露和声明边界 | 3.1.6 | 保留 |
| L-10 论文结果文字 | 3.1.7 | 合并（与 L-12、L-15） |
| L-11 可复现实验命令 | 5.3(a) | 合并（命令族公共块） |
| L-12 当前阶段结论 | 3.1.7 / 1.2 | 合并 |
| L-13 因果稳定化验证 | 3.1.5 | 删除重复，独有内容并入 |
| L-14 正式模型化整合 | 2.3 | 保留 |
| L-15 最终创新点定义 | 3.1.7 | 保留 |
| D-1 文档目的 | — | 删除（纯元信息） |
| D-2 暗光已解决什么 | 3.2.1 / 3.1.3 | 合并（stride-14 限制移入 3.2.1） |
| D-3 数据和评价协议 | 2.1 / 2.4 | 合并 |
| D-4 当前结论摘要 | 1.2 / 3.2.6 | 合并 |
| D-5 实验时间线 | 3.2.2–3.2.4 | 拆分为实验记录 |
| D-6 已做过不能重复 | 3.2.6 | 合并 |
| D-7 论文映射 | 4.1 / 4.3 | 合并入统一文献节 |
| D-8 历史阶段建议 | 3.2.4（记录 4/5/6/7） | 计划 → 实验经过 |
| D-9 当前研究状态 | 3.2.7 / 3.2.4 | 合并（并按支线拆分） |
| D-10 快速接手摘要 | 3.2.4–3.2.7 / 6.2 | 拆分为实验记录 + 门槛汇总 + 支线裁决 |
| D-11 记忆读取顺序 | 5.6 | 合并 |
| D-12 V4/seq11 交接入口 | 6.2.2 / 6.2.3 / 附录 | 合并 |
| F-1 模型数据边界 | 1.1 / 2.4 | 合并 |
| F-2 epoch10 与 DFR | 6.2.2 | 保留 |
| F-3 基线指标与口径 | 6.2.2 | 保留 |
| F-4 路线 A | 6.2.2 | 保留 |
| F-5 路线 B | 6.2.2 | 保留 |
| F-6 路线 C | 6.2.2 | 保留 |
| F-7 路线 D | 6.2.2 | 保留 |
| F-8 路线 E | 6.2.2 | 保留 |
| F-9 路线 F | 6.2.2 | 保留 |
| F-10 路线 G | 6.2.2 | 保留 |
| F-11 路线 H | 6.2.2 | 保留 |
| F-12 已修复实现问题 | 6.3 | 合并（补入新增 3 项） |
| F-13 路线总裁决 | 6.2.3 | 合并（加日期与支线限定） |
| F-14 唯一推荐下一步 | 6.2.3 | 改写为未执行的条件性后续 |
| F-15 最小读取清单 | 5.6 | 合并 |
| F-16 资源约定 | 5.4 | 保留 |
| F-17 外部结果文件索引 | 5.5 | 保留 |
| T-1 检索边界 | 4（引言） | 合并 |
| T-2 第一优先级文献 | 4.2 | 合并去重 |
| T-3 第二优先级文献 | 4.2 / 4.3 | 合并去重 |
| T-4 第三优先级文献 | 4.2 | 合并 |
| T-5 证据—论点映射 | 4.2 | 保留 |
| T-6 综合裁决 | 4.4 | 合并 |

### 2. OBB 观测可靠性与连续输出（27 个来源章节）

| 原锚点 | 新正文位置 | 处置 |
| --- | --- | --- |
| H-1 已确定研究范围 | 1.1 / 1.2 | 合并（与 R-1、P-1 重复） |
| H-2 已有工作与证据 | 1.4 / 补充边界 | 改写为三分类 + 数据角色边界 |
| H-3 要证明什么 | 1.2 | 合并 |
| H-4 缺少的验收材料 | 5.1 | 改写为已完成 / 部分完成 / 真正缺失 |
| H-5 深度分支边界 | 5.2 | 保留（指向 Webots 新节） |
| H-6 新对话首轮任务 | — / 2.3 | 删除指令，环境约束并入 2.3 |
| H-7 写作自检 | 5.3 | 合并 |
| A-1 当前结论 | 2.2（2026-09-07 历史段） | 合并并标注日期 |
| A-2 模型数据身份清单 | 2.2 | 保留 |
| A-3 只读通路统计 | 3.1 | 保留 |
| A-4 首轮审计协议 | 2.1.1–2.1.7 | 保留（拆为 7 节） |
| A-5 服务器导出需求 | 2.3 | 保留 |
| A-6 稿件盘点 | 5.4 | 保留 |
| A-7 下一授权步骤 | — / 2.3 末段 | 删除指令，判断规则并入 |
| P-1 当前研究边界 | 1.1 / 4.2 | 合并 |
| P-2 已确认结果 | 3.2 / 3.3 / 3.4 | 合并 |
| P-3 当前可继续使用 | 3.5 | 合并 |
| P-4 V5.3 唯一开发问题 | 3.4 | 保留 |
| P-5 开发与最终验证边界 | 3.4 | 合并 |
| P-6 允许的论文表述 | 5.3 | 合并（修正图像质量叙述） |
| R-1 已冻结研究边界 | 1.1 / 1.3 / 2.2 | 合并 |
| R-2 已确认开发结果 | 3.3 / 3.4 / 3.5 | 合并（SHA256 保留） |
| R-3 最终比较与接口 | 4（口径表）/ 4.1 | 保留 + 新增报告优先级 |
| R-4 后续大论文接口 | 4.5 | 保留 |
| R-5 统一入口 | 4.2 | 保留 |
| R-6 V2 收尾规则 | 4.3 | 保留 |
| R-7 论文指标 V3 | 4.4 | 保留 |

### 3. Webots 单目深度估计（31 个来源章节）

| 原锚点 | 新正文位置 | 处置 |
| --- | --- | --- |
| E-1 统一符号定义 | 2.1.1–2.1.4 | 保留（`{P}` 行加坐标系性质声明） |
| E-2 当前深度估计目标 | 1.1 / 2.2 | 合并 |
| E-3 数据文件与入口 | 3.1 | 保留 |
| E-4 相机标定数据 | 3.2 | 保留 |
| E-5 顶梁尺寸与参考平面 | 3.3 | 保留 |
| E-6 正式序列及职责 | 3.4 | 保留 |
| E-7 图像标签真值对应 | 3.5 | 保留 |
| E-8 GT 与姿态字段 | 3.6 | 保留 |
| E-9 公式及现有证据 | 2.4 / 4.1 | 删除重复（参数只在 2.4，数值只在 4.1） |
| E-10 检测结果现有状态 | 4.2 / 4.3 | 删除重复（与 G-9.2 同表） |
| E-11 数据完整性结论 | 3.7 / 6.1 | 合并 |
| E-12 论文写作可用表述 | 5.2 | 合并（与 G-12） |
| E-13 与现有文档的关系 | — | 删除（由阅读约定 + 附录承担） |
| G-1 最终路线与符号 | 2.2 | 合并（含坐标系适用条件） |
| G-2 原图 OBB 与内参 | 2.3 | 保留 |
| G-3 Raw-opt 公式 | 2.4 | 保留 |
| G-4 反投影 | 2.5 | 合并（加刚性 ≠ 重力对齐说明） |
| G-5 外参转换 | 2.5 | 合并 |
| G-6 摆角计算 | 2.6 | 保留（加物理解释前提） |
| G-7 虚拟铅垂保留边界 | 2.7 | 保留 |
| G-8 Webots 真值与 GPS | 2.8 | 保留 |
| G-9 实验结果与证据边界 | 4.1 / 4.2 / 4.4 | 合并 |
| G-10 七项问题处理结论 | 2.9 | 保留（新增第 8 行：仅澄清未闭环） |
| G-11 唯一实验顺序 | 6.1 | 保留 |
| G-12 论文简要表述 | 5.2 | 合并（加必要条件段） |
| C-1 证据边界 | 1.3 | 合并 |
| C-2 train_01 垂向标定 | 4.5 | 保留 |
| C-3 train_02 自旋审计 | 4.5 | 保留 |
| C-4 暂定标定文件 | 4.5 | 保留 |
| C-5 Pilot 数据处理 | 3.4 | 合并 |
| C-6 唯一下一步 | 4.5 | 被后续结果取代，保留一句演进说明 |

---

## 二、状态裁定表

「当前有效结论」只对相应支线成立；引用时必须带支线与日期。

| 主题 | 当前有效结论 | 历史实验及其最终裁决 | 未执行的条件性计划 |
| --- | --- | --- | --- |
| 检测前端身份 | Base V3 epoch9（K1 有框优先、DINO fallback；refiner 分数 `1.0` 非置信度）用于 OBB 主线 | — | — |
| DINO 组件身份 | native S14 + ROI classifier `alpha=0.5` + S7 disabled（`source_safe_interpolated_head.pth`） | — | — |
| 暗光大目标 | 冻结 DINOv2 分支 + `alpha=0.25` 因果稳定器恢复排序（`0/33 → 29/33 → 32/33`） | — | — |
| 小目标候选覆盖 | 高分辨率 S7 使 fixed `seq03_small` R@100 `55/64 → 64/64` | — | — |
| 小目标最终排序 | 未解决（仍 `50/64`） | affine / lane v1-v2 / quality suppression / 时序 v1 / quality head / static ranker / selective promotion V1-V2 / risk residual / contrastive adapter 逐个裁决为回退 `best_epoch=0` | 结构不同的 native-protected V2（source gate 通过前不读 target） |
| source gate 例外 | 阶段二B relative-quality 通过（691/312，lost=0）；protocol-23/24 margin `0.225` 通过（688/311，lost=0）；protocol-26 通过 bounded-risk gate（696/320，lost=1） | 三者均未通过三段 fixed target-dev | protocol-26 授权但未执行的 target-dev 诊断 |
| 联合前端 | 结构已实现（`crane_symeood_dino_unified_v1.py`） | — | 联合 source gate；配置与归档记录的位置权重冲突待核验 |
| seq11 融合支线 | — | V4 路由失败；refiner / dual-tower / causal V2 失败；retentive V3 仅 source promotion；V4+seq11-v2 epoch10 为暂定候选（`source_gate_passed=False`） | 251 帧三折时序块 OOF/CV |
| OBB 分量策略 | 三重固定比较：Base V3 原始输出 / 简单置信度拒绝 / V5.1 | V5.2、V5.2.1、V5.3 全部停止 | 未知新序列验证（当前无一） |
| OBB 报告链 | V2 真实在线评估与无 GT CSV；V3 论文制表字段；`fixed_test_obb_paper_final_report_v1` 为论文固定指标来源 | V1 为缓存回放参照 | — |
| Webots 标定路线 | Raw-opt（Train-03 拟合，Fixed-dev-01 GT OBB 组件诊断） | 早期幂律 `k, alpha` 已被 Raw-opt 取代；Unknown-02 属旧 Plumb/Opt | 检测框来源与 `depth_valid` 规则的冻结 |
| Webots 三维链 | 只有条件接口；标量深度闭合不等于三维位置与摆角已验证 | — | 姿态不变证据或逐帧动态旋转；逐帧三维姿态与可见性字段 |

---

## 三、本轮纠正的状态冲突

| 冲突 | 原表述 | 纠正 |
| --- | --- | --- |
| 「统一推理入口未完成 / 完整 final test 未完成」 | DINO 文档文首与「系统级未闭环问题」按全项目陈述 | 按支线拆开：Base V3 观测链的在线入口与固定 TEST 报告已完成（OBB 文档第 4 节）；「未完成」只限 DINO/S7 支线与联合前端 |
| 「所有 learned S7 分支均回退 best_epoch=0」 | 抹掉了通过门槛的实验 | 新增门槛汇总表，分列 source exact gate / bounded-risk gate / target 诊断 / 最终保留状态；阶段二B 与 protocol-23/24 通过 exact gate，protocol-26 通过 bounded-risk gate |
| 「统一从 epoch_24 初始化并训练 24 epoch」「checkpoint 只按 16/18/20/22/24」「必须使用全新 work_dir」 | 写在「统一实验纪律」，被当作全项目规则 | 改为按支线说明 + 逐条核查表：config 层 `load_from` 全为 `None`；候选 epoch 集只属 BrightAug 的 `ckpt_sweep`；`--seed 0` 仅对 `tools/dist_train.sh` 与 labeller 入口成立；work_dir 只作为各实验事实 |
| 「最终模型待绑定」 | 与已明确的 Base V3 epoch9 共存 | 改为「2026-09-07 的身份保留状态（历史）」+「当前前端身份（2026-09-10 起）」，并指明 V4+seq11-v2 归检测融合支线 |
| 「未发现该新方案的完整实现」 | 列在证据边界表 | 1.4 改为已完成 / 部分完成 / 真正缺失 / 不再作为方向四类 |
| 「图像质量特征具有进一步开发价值」 | 出现在允许表述中 | 更正为「已被裁决 `STOP_IMAGE_QUALITY_CANDIDATE`，只作失败边界，不是当前方向」 |
| V1/V2/V3 都被称为指标来源 | 三个版本并列 | 第 4 节开头新增用途与优先级表，明确 V1 参照、V2 系统评估、V3 论文表格字段、`fixed_test_obb_paper_final_report_v1` 论文固定指标来源，并规定两套 `R_center` 口径不可混用 |
| 刚性安装 = 相对重力姿态固定 | 公式与七项问题表按「已解决」处理 | 在 2.1.1、2.2、2.5、2.6、2.9、5.2 写明两者区别与必要条件；七项问题表新增一行标注「仅澄清、未闭环」 |
| Unknown-02 结果在两处重复 | 4.1 与 4.6 | 数值只留 4.1，降级规则并入其后，删除 4.6 小节 |
| 论文表述重复三处 | 2.9 简短版 + 5.2 完整版 + G-12/E-12 合并版 | 只保留 5.2，2.9 留指针 |
| 「当前最值得继续」「下一步只允许」无范围 | 逐条命令式 | 加支线与日期；6.4 新增「表述必须带支线与日期」表 |
| 归档 manifest 被当作当前映射 | 附录仅说明「记录目标位置」 | 三份文档附录 + README 均声明其为首次整合快照，字段不对应当前正文；新映射移至本文件 |

**第三轮局部纠错（同日，仅三项）**

| 位置 | 原表述 | 纠正 |
| --- | --- | --- |
| DINO 3.2.7 支线裁决 | 「各 learned S7 分支正式 exact-retention gate 均未通过」 | 改为按 source gate / target-dev / 最终采用状态三层分列：阶段二B（691/312，lost=0）与 protocol-23/24（688/311，lost=0）**通过** exact gate，protocol-26（696/320，lost=1）通过 bounded-risk gate；三者均未通过三段 target-dev；正式 deployable 仍是 native S14 `alpha=0.5`、S7 disabled。同时把 3.2.6「safe 模型则无 small Top-1 增益」改为「source 有 small Top-1 增益、但 target-dev 未提高」 |
| OBB 5.3 可以表述 | 把 pooled source-val 的 `1/5`、`0/5` 拼成一组并挂在 V5.3 名下 | 拆为两条互不合并的记录：早期图像质量分支（pooled source-val；相对 V5.1 长/短/联合各 3/5；相对置信度 real 2/5、sim 0/5）与 V5.3 正式 OOF（相对已有 11 个在线尺度风险特征 real 1/5，三折一折胜多于负，`STOP_IMAGE_QUALITY_CANDIDATE`）；并标明各自 SHA256 与比较对象 |
| Webots 2.1.1 / 2.2 / 2.5 / 2.6 / 2.9 / 5.2 / 6.1 | 「刚性安装 ⇒ 固定外参」，固定/动态未分情形 | 改为三情形：情形 1（重力对齐 + 姿态不变）才可称固定外参；情形 2（姿态变化 + 逐帧旋转证据）必须用 \(R_{P\leftarrow C}(t)\) 与 \({}^C\mathbf d_{CP}(t)\) 动态变换，不得称固定外参；情形 3（固连坐标系）只能报相对几何。平移向量须核对表达坐标系与时间依赖性；三维位置与摆角仍标注**未验证** |

---

## 四、无法核实、本轮不做裁定的内容

1. **联合前端的位置权重**：归档记录（2026-08-08）称 SymEOOD 位置权重为 `work_dirs/crane_symeood_k1/best_Weighted_R_center_epoch_12.pth`，基座为 `crane_symeood_k1.py`；当前 `crane_symeood_dino_unified_v1.py` 的 `_base_` 为 `crane_symeood_k1_brightaug.py`、位置权重为 `work_dirs/crane_symeood_k1_brightaug/epoch_20.pth`，且 `work_dir` 写作 `..._unified_v2/full_test`。两侧证据不能同时成立，本轮只并列记录（见 DINO 文档 6.2.1）。
2. **Base V3 epoch9 teacher 的 checkpoint SHA256**：归档明确「从 promotion report 动态读取，不猜测」，本机未绑定该工件。
3. **K1 fixed-dev 原始结果文件**：`predictions_full.jsonl` 及其 manifest、`inference_full.log`、`evaluation_vs_k1.json` 不在可检索路径，858/893 帧 `q` 越界只能作为阶段性诊断。
4. **`seq03_small` 的 MCML 口径**：固定切片 RIoU 口径记 6，系统 evaluator 口径记 14，差异未消除。
5. **epoch10 相对 Base V3 的 DFR 是否退化**：归档明确缺可直接重算的对照。
6. **本工作区缺失的工件**：`work_dirs/crane_symeood_k1_brightaug/` 为空目录，`work_dirs/*unified*` 不存在；因此联合前端与 ScopedDINO 完整 test 命令在本工作区无法复跑。
7. 未打开 `main.tex`、`document.tex`、背景 md 等外部稿件文件。

以上各项均不声称已验证；权重、外部实验结果与文献均未复核。

---

## 五、已执行 / 未执行的检查
**已执行**

- 完整阅读三份主文档（本轮改写前 3,280 行）与归档 11 份原文（合 337,536 字节中的归档部分）。
- 逐锚点核对 108 个来源章节在本轮正文中的唯一去向（本文件第一节）。
- 归档完整性：11 个文件 SHA256 与 manifest 全部一致；归档目录未被写入。
- 数字完整性：对归档原文的多位数 token 做存在性比对，剩余未逐字出现项全部为旧章节号、旧路径或排版差异（如 `132609` 写作 `132,609`）。
- 标识符完整性：对归档中的反引号标识符做存在性比对，剩余项为旧路径、旧小节号与空格差异。
- 只读配置核查（本轮新增）：`crane_project/configs/crane_symeood_dino_unified_v1.py`（139 行）、`crane_symeood_k1_brightaug.py` 训练管线、`crane_symeood_formal_dino_native_s14_v1.py`、`tools/dist_train.sh`，以及相关 `work_dirs` 的存在性。
- 结构检查：三份文档锚点无重复，文内 `#anchor` 链接全部可解析，代码围栏成对，表格列数与表头一致，无遗留占位符。

**未执行**

- 未运行任何训练、推理、评估脚本；未重算任何深度、RIoU、覆盖率或误差。
- 未重新核验权重、checkpoint SHA256、预测 JSON、CSV。
- 未联网核验文献 DOI / 页码 / 结论。
- 未修改归档原文；未修改记忆文件。
