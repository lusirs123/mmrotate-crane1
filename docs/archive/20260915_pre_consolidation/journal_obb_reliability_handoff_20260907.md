# 小论文进度与新对话交接：OBB观测可靠性与连续输出

更新：2026-09-07。本文记录用户在本对话中最终确定的范围，以及本机交接文档可支持的进度。不是模型性能验收报告。模型融合另一对话若有更新，应读取其实际产物后更新模型身份；不能把本文列出的历史候选当作最新最终模型。

## 1. 已确定的研究范围

小论文主线：复杂港口图像序列 → SymEOOD抓斗旋转检测 → OBB分量可靠性判定 → 连续视觉观测输出。

研究对象为抓斗顶梁OBB，纯RGB单目，不使用PLC、编码器。用户已经选择“OBB观测可靠性与连续输出”作为新增研究方向。中心、尺度、图像方向分别评价可靠性；新增贡献尚待实验成立。

相机共模补偿不再是必做模块。仿真—真实混合训练已经存在，不因数据混合本身就宣称新贡献。Raw-opt米制深度、相机—吊点外参、物理摆角、EKF属于大论文后半部分，不阻塞小论文。

大论文建议层次：检测 → 可靠二维观测 → 单目深度与物理状态估计。小论文主要覆盖前两层。

## 2. 已有工作与证据

| 内容 | 可确认状态 | 限制 |
| --- | --- | --- |
| SymEOOD/K1及DINO相关检测 | 已有实现、训练和多轮实验记录 | 不同模型和split指标不能拼接；最终小论文版本待绑定 |
| 检测与时序评价 | 已有RIoU、中心指标、DFR、ACI、MCML等基础 | 指标定义与误差/运动的关系要核对；平滑不自动代表准确 |
| source训练/验证数据 | 历史协议train+train_sim共2781帧，official source-val 738帧 | source包含真实与仿真；source-only不等于sim-only |
| seq02/seq03困难片段 | 已用于诊断，已有正负结果 | 已暴露片段不是全新未知序列；不重开旧S7/router实验 |
| seq11-v2 | 251帧，历史203/48划分 | 同视频辅助验证不等于独立泛化；48帧困难支持缺失 |
| 几何误差诊断 | 已发现存在性、中心、尺度质量不一致 | 是新方向动机，尚未证明可靠性估计有效 |
| 新的分量可靠性方法 | 用户已选定研究方向 | 未发现该新方案的完整实现、冻结规则及验证结果 |
| 连续观测接口 | 已讨论测量/预测/不可用区分 | 统一接口及漏检恢复策略尚待实现验证 |
| 小论文稿件 | 本轮未审查独立写作目录 | 不宣称全文为空或已完成；新对话应先盘点已有草稿 |

检测历史入口：`docs/20260905_symeood_dino_fusion_seq11_full_handoff.md`，应完整读取。该文档把V4+seq11-v2 epoch10列为source-val暂定候选，缺少困难OOF支持；它不能直接升级为最终模型。用户说模型改进基本停止，意味着不自动重开训练，不意味着该候选通过全部验收。

另一条正式native-S14入口为 `crane_project/configs/crane_symeood_formal_dino_native_s14_v1.py`，其纯DINO身份与SymEOOD K1、融合候选不同。不能把深度诊断所用native-S14自动作为小论文的“SymEOOD模型”。

## 3. 新方向具体要证明什么

原图输入 B_t=(u_t,v_t,w_t,h_t,gamma_t,s_t)。候选输出为中心、尺度、方向三个可靠性量 r_center、r_scale、r_angle，以及对应有效标志。用r表示可靠性，避免与深度公式q冲突。

核心问题：在相同保留覆盖率下，能否比单纯检测置信度筛选更有效地减少中心、长短边、周期方向误差？

第一阶段保留原始预测框，不做候选接管、不改检测几何。可靠性用于接受/拒绝观测；角度等价表示规范化属于必要实现，不单独包装为创新。近正方形方向退化、边界截断、尺度跳变、时序残差是候选线索，不是已经验证的质量规则。

GT只用于离线标签和评价。检测器在训练图像上可能过于准确，因此先审计错误样本支持，再决定采用独立校准集还是OOF预测；不能直接利用已暴露target逐帧设计规则。时序推理只用当前及过去帧，按完整序列/时序块隔离，必要时清除边界历史。

连续输出区分measurement、prediction、unavailable；先完成逐帧可靠性再增加有界连续观测管理。不要求第一版使用Kalman/EKF，不以复制历史框伪造检出。未补偿相机运动时，图像位移包含相机运动，不称为物理摆动。

## 4. 缺少的验收材料

1. 小论文检测器唯一身份：config、checkpoint、SHA256、坐标/角度约定、推理阈值、各split产物。
2. 可复用原图—GT—预测逐帧对照表，包括序列与时间戳；先定位已有输出，避免重复推理。
3. 中心/尺度/方向错误的训练侧支持审计；检测置信度与各分量误差关系。
4. 新可靠性规则/校准方法、分量有效标准、全部超参数选择来源。
5. 等覆盖率误差与risk-coverage曲线、错误接受率、正确拒绝率及连续不可用长度；所有帧含漏检必须计入覆盖率。
6. 置信度筛选基线、几何特征增量、时序特征增量、分量与整体判定消融；同时观察误差、覆盖率和延迟。
7. 冻结后的独立序列验证，以及真实视频时延/资源占用。旧target只按其真实暴露历史报告。
8. 文献新颖性核查与稿件整理。不能因为新增模块有名字或指标改善就保证创新性/发表。

## 5. 深度分支的边界和纠正

Raw-opt标量深度配对修复不代表完整三维链闭合。用户提供的新审计指出：三维反投影尚未闭环、动态重力坐标转换缺失、depth_valid尚未冻结；固定旋转仅在相机相对重力固定等条件下适用。其姿态变化具体数值本轮未重新计算。

深度文档仍存在Unknown-02历史定位残留，需要另行一致性修订；不要为小论文继续实现三维外参/EKF。Train-03是深度标定序列，不自动成为可靠性方法的校准数据；若使用必须明确新协议职责和既有暴露。

相关文档：`docs/Webots 单目深度几何契约.md`、`docs/Webots深度估计现有数据与论文证据清单_20260906.md`。其中旧“完整修复”措辞应以本节限制解读。

## 6. 新对话的首轮任务

先读取本文件及完整检测交接，然后盘点模型融合对话的最新有效结果、可用checkpoint身份、source侧GT与逐帧预测、以及 `/Users/mac/Desktop/lunwenxiezuo` 下已有小论文稿件。用户背景在 `/Users/mac/Desktop/大论文研究内容/背景.md`，按需读取。

交付一份可复用数据/模型清单和OBB分量可靠性首轮审计方案。若本机已有完整、角色明确的预测与GT，可以直接进行只读误差统计；如缺失，列出精确缺项和服务器导出命令。第一轮不训练检测器、不重跑旧实验、不读取未知集，不把source-val或fixed-dev改名成训练集。

服务器仓库：`/media/omnisky/personal_files/ljj/symEOOD`。本机通常无权重，优先复用服务器预测，后续推理在服务器执行。

验收：先回答哪些错误可识别、哪些分量有训练/验证支持，随后决定是否实现可靠性方法。不要承诺该方案必然优于置信度基线。

## 7. 写作自检

- 贡献：检测有历史证据；可靠性是待验证贡献。
- 表述：二维图像方向不是机械自旋/物理摆角；连续输出不是连续有效测量。
- 实验：模型、split、阈值来源、覆盖率必须可追溯。
- 完整性：同视频OOF不能替代未知序列；本轮未审查稿件完成度。
- 方法：可靠性筛选不能修复错误几何，必须计入拒绝代价。
